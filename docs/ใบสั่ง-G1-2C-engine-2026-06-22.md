# ใบสั่ง dev — G1 ก้อน 2C (engine · เสี่ยงสุด) · 22 มิ.ย. 2026

> เตรียมไว้ก่อน · **ยังไม่เริ่มแก้** (รอแชทอื่นที่แก้ index.html จบ + พี่นัทเคาะ openQuestions ด้านล่าง)
> เจาะโค้ดจริง `public/calculator/index.html` แล้ว (workflow `wliggywfo.output`) · ทุก line ref ตรวจแล้ว
> กฎ: ก้อนละ `golden-snapshot 150/150` + เทสเวปจริง (2 โหมด + G6 ถ้าแตะ DOM ร่วม) + push · 1 ก้อน = ≥1 commit

---

## ✅ Audit ดราฟ G1 ทั้งหมด vs คิว (22มิ.ย. · ทุก item ที่ออกแบบ/คอนเฟิร์มต้องอยู่ในคิว)
- DRAFT-G1-doortypes (15 ชนิด · ฟอร์ม/ออปชั่น/โซน) → ก้อน 2A/2B/2C + #8 ✅
- DRAFT-G1-บานผสม+เฟี้ยม:
  - บานผสม 3 ชั้น (ปรับ/จัดเต็ม) → **มีใน index.html แล้ว** ✅
  - เฟี้ยม แยกกลาง→ช่องซ้าย/ขวา (o-folddir+fold-split L4620) → **มีใน index.html แล้ว** ✅
  - **L3 กล่องสี/กระจก (ข้อนี้) 3 ชั้น → ยังไม่มีในคิว → เพิ่มเป็นก้อน #9** (พี่คอนเฟิร์มแล้ว)

## ⚠️ ต้องเคาะก่อนเริ่ม (blocking) — เรียงสำคัญ

**A. ชุดล็อค (ก้อน #2 — กำหนดขอบเขตทั้งหมด):**
1. **VALUE ที่เก็บ** ใช้ชุดไหน — (ก) ชุด o-lock `ไม่มี/มีล็อค/มีล็อค+กุญแจ` · หรือ (ข) คงชุด slidelock `มีกุญแจ/ไม่มีกุญแจ` แล้วเติมตัวที่ 3 · *แนะนำ (ก) ให้ทุกที่ใช้ value เดียว แต่ต้องตามแก้ print 2 จุด (L6544/6645) + G6R lockDef ~13 ตัว*
2. **DEFAULT** ปุ่มไหน on — เดิม o-lock=`ไม่มี` · slidelock=`มีกุญแจ` · บานเลื่อน/PC ใบจริง = "มีกุญแจ" มาตรฐาน · จะตั้ง default บานเปิด/เฟี้ยม/กระทุ้ง/หมุน เป็นอะไร? (กระทบข้อความใบ ไม่กระทบราคา)
3. **คำมาตรฐาน** "ชุดล็อค" (ค.การันต์) หรือ "ชุดล็อก" (ก ไก่) — โค้ดปนกัน
4. รวม optSel คีย์เดียว หรือคงแยก (lock vs slidelock) · *แนะนำคงแยก รอบนี้ทำแค่หน้าตา/คำ/3 ตัวเลือกให้เหมือน*
5. G6R lockDef ~13 รุ่น (L7220-7238) — รวมแก้ก้อนเดียวหรือแยก

**B. แขนค้ำ (ก้อน #1):**
1. เกณฑ์ auto-tick — มีแค่ใน CHECKLIST (กว้าง>0.6 และ/หรือ สูง>1.2 · L2099 · เป็นข้อความแนะนำ ไม่ใช่ spec) · ใช้เกณฑ์นี้ได้ไหม / "และ" หรือ "หรือ" / เทียบกว้างต่อบานหรือกว้างรวม · **หรือพี่มีเกณฑ์จริงจากหน้างาน?**
2. "+500/ชุด" = ต่อบาน (×จำนวนบาน) หรือ ต่อรายการ (คงที่ 500)
3. auto = บังคับติ๊ก (ปลดไม่ได้) หรือ default แนะนำ (ปลดได้)
4. item เก่า `awning_brace` → ลบ หรือ `hidden:true` · *แนะนำ hidden (golden นิ่ง)*

**C. ดัดโค้ง (#4):** เลือก "หน้าต่าง" แล้วชื่อใบ (curved_fixed/slim ไม่ขึ้นต้น "ประตู") — เปลี่ยนชื่อตามประเภท หรือปล่อยชื่อรุ่นเดิม · ปลดล็อกครบ 4 รุ่น (รวมติดตายโค้ง) หรือเฉพาะบานเปิดจริง
**D. เพิ่มล็อค (#5):** default 3 ชนิดใหม่ = "ไม่มี" หรือประตูให้ "มีกุญแจ" · ดัดโค้งใส่ครบ 4 รุ่นไหม (curved_fixed ติดตาย ควรไม่มี?)
**E. YKK (#3):** ชื่อลงใบตัว "มี +5,000" = "โช้คอัพ" หรือคำเฉพาะ · ตัดหน้าต่าง = ซ่อนปุ่ม vs ล็อกเทา
**F. บานเปิด zone2 (#6):** "โซน②" = กล่อง "🔧 ล็อค·มุ้ง·คาด" ใช่ไหม · ย้าย "ช่องแสง" ด้วยไหม (ตอนนี้อยู่โซน① เห็นง่ายแล้ว) หรือย้ายแค่ ตารางเต็มบาน+combo
**G. บานยก (#7):** เจออาการ "เลือกประตูได้" จริงไหม หรือแค่ปิดงาน (โค้ด lock window แล้ว)

---

## ลำดับทำปลอดภัยสุด (label-only/cosmetic ก่อน → กระทบราคา/value ท้าย)

1. **#7 บานยก** — verify เฉยๆ ปิดงาน (ไม่แตะโค้ด · บานยกอยู่ ITYPE_LOCK_WINDOW L5924 แล้ว)
2. **#5 เพิ่มล็อค บานยก/ดัดโค้ง/frameless_door** — 1 commit · gate เดียว label-only · golden นิ่ง
3. **#4 ดัดโค้งปลดล็อก itype** — 1 commit · ลบ 'ดัดโค้ง' จาก ITYPE_LOCK_DOOR (itypeFor L5928)
4. **#8 จัดโซนทุกชนิดให้ตรงดราฟ (รวม #6 บานเปิด)** — cosmetic · ไล่เทียบ ดราฟ①②③ vs CATS ทุก 15 ชนิด → ย้ายตัวอยู่ผิดโซน · golden นิ่ง · แยก commit
5. **#3 YKK** — 2 commit: (A) closer 2 ตัว (gate cat='YKK') · (B) ตัดหน้าต่าง (.itype-seg + เทส G6)
6. **#2 รวมชุดล็อค** — commit ใหญ่เดี่ยว (หลังเคาะ A) · ห้ามรวมกับอื่น
7. **#9 L3 กล่องสี/กระจกต่อข้อ (3 ชั้น)** — feature · เทียบ/reuse G2 g2FinDrill (มีโครง 3 ชั้นแล้ว) · ⚠ สีพิเศษ/อบพิเศษ บวกเงิน → default ต้องคง "ตามทั้งใบ ไม่เพิ่มเงิน" (golden นิ่ง) · แยก commit · เทสราคา
8. **#1 แขนค้ำ** — 2-3 commit: (A) checkbox+engine FULL · (B) auto-tick UI · (C) บั๊กคิดเร็ว G6 ex_br · กระทบ golden → rebaseline ทีละชั้น

---

## รายก้อน — จุดโค้ด + แก้อะไร + golden/เสี่ยง

### #7 บานยก หน้าต่างเท่านั้น — ⚪ no-op (verify)
- บานยกอยู่ ITYPE_LOCK_WINDOW (L5924) แล้ว · itype ไม่เข้าสูตรราคา (method lift · L1264) · **แค่เทสยืนยัน** ว่าโหมดเต็ม/คิดเร็วเลือกประตูไม่ได้

### #5 เพิ่มชุดล็อค (label-only) ให้ บานยก/ดัดโค้ง/บานเปลือยประตู — 🟢 ต่ำ
- o-lock render L4648 เงื่อนไข `['บานเปิด','บานเฟี้ยม','บานกระทุ้ง','บานหมุน'].includes(p.cat)` → เพิ่ม `'บานยก','ดัดโค้ง'` + เงื่อนไข `p.id==='frameless_door'`
- ⚠ ห้ามใช้ `p.cat==='บานเปลือย'` (จะโดน frameless_fixed ด้วย) → ใช้ p.id
- ⚠ curved_fixed (ติดตายโค้ง) ถ้าไม่ต้องล็อค ต้อง gate p.id ไม่ใช่ cat
- read/print generic ไหลเอง (L6149) · calcUnit ไม่อ่าน → golden นิ่ง

### #4 ดัดโค้ง ปลดล็อก itype — 🟡 กลาง-ต่ำ
- ITYPE_LOCK_DOOR (ใช้ที่เดียว itypeFor L5928) → เอา 'ดัดโค้ง' ออก
- golden นิ่ง (snapshot ไม่แตะ .i-type · default door คงเดิม)
- openQ: wording ชื่อใบเมื่อเลือกหน้าต่าง (L6932)

### #8 จัดโซนทุกชนิดให้ตรงดราฟ (รวม #6 บานเปิด) — 🟢 cosmetic · golden นิ่ง
- เป้า: ทุกชนิดบาน เนื้อในโซนตรงดราฟ ①สเปก / ②ออปชั่นหลักใช้บ่อย / ③เสริม(พับ)
- วิธี: ไล่เทียบ ดราฟ DRAFT-G1-doortypes ①②③ กับ CATS keyword grouping (L3132+ · disp1 ติดตาย·ช่องแสง / disp2 ล็อค·มุ้ง·คาด / disp3-5 โครงสร้าง·เสริม·หมายเหตุ) ทุก 15 ชนิด → ตัวที่อยู่ผิดโซน ย้าย keyword ระหว่าง CATS groups
- ⚠ CATS = groupGHOpts (L5176) โค้ดร่วม G1-G7 · ย้าย keyword ต้องไม่กระทบกลุ่มอื่น (เช็ค match ปลายทาง · `combo บาน` มีใน CATS[3] L3147 อยู่แล้ว)
- ไม่แตะราคา/สูตร · read/calc อ่าน class ไม่พึ่งตำแหน่ง → golden นิ่ง
- รวม #6 เดิม (บานเปิด ช่องแสง/ตารางเต็มบาน/combo → ②ใช้บ่อย)

### #6 (เดิม) — รวมเข้า #8 แล้ว
- CATS (groupGHOpts L5176) — เติม keyword ของ 3 ตัวเข้า CATS[1] (โซน② ใช้บ่อย)
- ⚠ **เช็คซ้ำ:** `combo บาน` มี keyword ใน CATS[3] (L3147 · '🏗️ โครงสร้าง·ราง') อยู่แล้ว → ต้องให้ CATS[1] match ก่อน + verify match ปลายทางจริง (JSON เดิมบอกตก catch-all = คลาด)
- ไม่แตะราคา/สูตร · golden นิ่ง

### #3 YKK ประตูเท่านั้น + โช๊ค มี/ไม่มี — 🟠 สูง
- **(A) closer:** o-closer L4647 ใช้ร่วม 5 บาน (casement_euro/xseries/flush_solid/inset_solid + YKK) → **ต้อง gate `p.cat==='YKK'`** ให้ YKK เห็น 2 ตัว (ไม่มี / มี +5,000) เท่านั้น · บานเปิดคงเดิม 4 ตัว · ห้ามแตะ option แรก value=0
- **(B) ตัดหน้าต่าง:** .itype-seg (render L4279-4290) — gate cat='YKK' ล็อกประตู · **dev ต้องเปิด render seg จริงก่อนเขียน old→new** (READ-only นี้ผลถูกตัด) · แตะ DOM ร่วม → **เทส G6 ด้วย**
- golden นิ่ง (snapshot ไม่แตะ closer · default ไม่มี)

### #2 รวมชุดล็อค 3 แบบทุกบาน — 🔴 สูง (หลังเคาะ value scheme)
- ระบบ a (o-lock) L4648-4651: บานเปิด/เฟี้ยม/กระทุ้ง/หมุน · ป้าย "ล็อค" 3 ตัว → เปลี่ยนคำ "ชุดล็อค" + label ไม่มีชุดล็อก/มีชุดล็อก/มีชุดล็อก+กุญแจ
- ระบบ b (o-slidelock) L4722-4725 (บานเลื่อน/เลื่อนภายใน) + L4730-4734 (PC) · 2 ตัว → 3 ตัว · **แก้ render 2 จุดพร้อมกัน**
- print ลงใบ: L6149 (o-lock "ล็อค:") + L6544/L6645 (o-slidelock "ชุดล็อค (...)") + L7523 (G6R chip)
- G6R L7361 pick() + lockDef L7220-7238 (~13 รุ่น) — ตามแก้ map ค่า
- calcUnit ไม่อ่าน → golden นิ่ง (แต่ default ที่เปลี่ยน = ข้อความใบเปลี่ยน · golden วัดยอด ไม่จับข้อความ ระวังใบเพี้ยนเงียบ)

### #1 แขนค้ำ checkbox + auto — 🔴 เสี่ยงสุด (golden ขยับ)
- เดิม item แยก awning_brace (L770 · cat 'เสริม' · +500 · เปิดล่าง) → ทำ checkbox ในฟอร์มบานกระทุ้ง
- ⚠ **golden พังถ้า auto-tick default ON:** golden FULL G1 = 1.5×2.0 (L55) เกินเกณฑ์ 0.6/1.2 → `1:awning_euro 24,610→25,110` + `Q1:base 23,000→23,500`
- **กัน:** auto-tick เป็น UI-only (oninput) ไม่ใช่ค่า default ของ readItem · harness ไม่ trigger oninput → checked=false ตอน snapshot
- ห้ามใส่ใน p.opts ของ awning_euro (render/read/calc loop generic จะบวกอัตโนมัติ) · ลบ item → คง `hidden:true` (เหมือน truss_beam_cover L768)
- พ่วงบั๊ก undercharge ฝั่งคิดเร็ว G6 (ex_br pill ไม่บวกเงิน L7385)

---

## กับดัก golden (ทุกก้อน)
- ห้ามแตะ L4647 (o-closer +5,000) / L4718 (o-track ซ่อนราง +5,000) ที่อยู่ติดบล็อกล็อค
- helper generic (rfChips/chSetChip/groupGHOpts) ห้ามแตะตัว helper — แก้แค่ argument
- แตะ .itype-seg / DOM ร่วม = ต้องเทส G6 (G6 ซ่อนฟอร์มด้วย CSS ครอบ wrapper · เคยพัง)
- ก้อนกระทบ golden (แขนค้ำ) → rebaseline ทีละชั้น มี log ว่าตัวไหนเปลี่ยน
