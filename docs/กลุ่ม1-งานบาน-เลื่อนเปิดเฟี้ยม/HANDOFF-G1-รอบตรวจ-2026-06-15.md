# 📤 ใบสั่ง dev — G1 รอบตรวจ (2026-06-15) → แชท B

> จากการตรวจ `node scripts/check-g1.mjs` เทียบดราฟ G1 · เคาะกับพี่นัทครบแล้ว · เหลือ 3 งาน
> กติกา: 1 ธง = 1 commit เล็ก · รันเทสก่อน-หลังทุกธง · index.html แก้ทีละแชท

## ✅ ตรวจแล้วทำตรงดราฟ (ไม่ต้องแตะ)
รุ่นครบ 6 กลุ่ม · ผนังเบาในกลุ่ม 1 · คาดตารางเปิดให้ติดตาย/เปลือย · มุ้ง 14 แบบ (imp28+imp30) · เฟี้ยมเปิดซ้าย/ขวา · บานเปลือยสีเฟรม · ราคาทุกรุ่นตรง engine · o-solidlight (ช่องแสง) **เก็บไว้ถูกแล้ว**

## 🎯 3 งานที่ต้องทำ

### ธง 1 — ตัด o-solidlower (แผ่นทึบล่าง) ทิ้ง [เล็ก · ทำก่อน]
- **เหตุ:** มติพี่นัท 15 มิ.ย. = ตัดทิ้ง (ซ้ำกับลูกฟูก · ใช้ลูกฟูกเป็นออปชั่นแทน) · **คนละตัวกับ o-solidlight (ช่องแสง) ที่ให้เก็บไว้**
| ช่อง | รายละเอียด |
|---|---|
| 🎯 จุด | render ของ `o-solidlower` + `o-sl-color`/`o-sl-w`/`o-sl-h`/`o-sl-wrap` ใน buildItemOpts (G1 path · ตอนนี้ยังโผล่ เช่น ykk_vent) |
| 🔍 ของเดิม | label/select แผ่นทึบล่าง (o-solidlower) ที่ยัง render |
| ✏️ แก้เป็น | ลบ render ทิ้ง (เก็บ logic ราคา dormant ได้แบบที่ G6 ทำ — readItem อ่าน DOM ไม่เจอ → optSel.solidlower='') |
| ✅ เทส | render ทุกบาน G1 ไม่มี `.o-solidlower` · `node scripts/check-g1.mjs` ไม่ขึ้น solidlower · golden 143 ตรง |
| 🚩 flag | - (ตัด render เฉยๆ) |

### ธง 2 — YKK เพิ่มครอบวงกบ + ดรอปพื้น (เหมือนบานอื่น) [เล็ก]
- **เหตุ:** มติพี่นัท 15 มิ.ย. = YKK ต้องมีครอบวงกบ+ดรอปพื้นเหมือนบานทั่วไป (เดิมสเปกเก่าว่า YKK ไม่มีครอบวงกบ — ยกเลิก)
- **สาเหตุที่หาย:** `AREA_CATS` (L890) มี `'YKK'` → `areaOnly=true` (L3145) → บล็อกครอบวงกบ+ดรอปพื้น (L3314) ถูกข้าม
| ช่อง | รายละเอียด |
|---|---|
| 🎯 จุด | L3314 (เงื่อนไขเปิดบล็อก ครอบวงกบ + ดรอปพื้น) |
| 🔍 ของเดิม | `if(!areaOnly && !p.zipscreen && !p.handrail && !['shower','บานเปลือย'].includes(p.cat)){` |
| ✏️ แก้เป็น | `if((!areaOnly \|\| p.cat==='YKK') && !p.zipscreen && !p.handrail && !['shower','บานเปลือย'].includes(p.cat)){` |
| ✅ เทส | render ykk_vent → มี `.o-fcsides` + `.o-dfm` · ดรอปพื้น 8ม. = +5,000+(8-7)×750 · `check-g1` ไม่ขึ้น 🔴 YKK |
| 🚩 flag | - |
- **หมายเหตุ:** o-dfm inner-condition (L3315) ไม่ได้ exclude YKK อยู่แล้ว · o-fcsides (L3314) ก็ไม่ exclude YKK → พอบล็อกเปิด ทั้งคู่ render เอง · เช็คว่า ykk อื่น (ykk_exhido bucket / tostem) ได้ด้วย

### ธง 3 — ทำ 6 ปุ่มชนิดบานแบบดราฟ [✅ dev ทำแล้ว · มีผลข้างเคียง ดูธง 4]
- ✅ dev ทำ `G1GROUPS` (L2662) แล้ว — 6 ปุ่ม บานเลื่อน/บานเปิด/เฟี้ยม/ติดตาย-ทึบ/ดัดโค้ง/อื่นๆ · ทำงานได้
- **เหตุ:** ดราฟ G1 ใช้ 6 ปุ่มใหญ่→รุ่นย่อย (เหมือนกั้นห้อง G6) · ตอนนี้ G1 ยังเป็นเมนู cat เดิม (รุ่นเลือกได้ครบแล้ว แค่ยังไม่จัด 6 ปุ่ม)
| ช่อง | รายละเอียด |
|---|---|
| 🎯 จุด | selector ชนิดบาน G1 ใน buildItemOpts (famSelectorHTML/FAM_CATS path) |
| 🔍 ของเดิม | เมนู FAM_CATS['1'] (10 cat) |
| ✏️ แก้เป็น | 6 ปุ่ม profile-driven แบบ **G6GROUPS/G6PROF** (ดู index.html G6 + `docs/กลุ่ม1.../HANDOFF-G1-FINAL-dev` ข้อ 3) · ปุ่ม: บานเลื่อน/บานเปิด/บานเฟี้ยม/ติดตาย-เปลือย/ดัดโค้ง/อื่นๆ-พิเศษ |
| ✅ เทส | `check-g1` ขึ้น 🟢 "6 ปุ่ม (G1GROUPS)" · รุ่นครบทุกกลุ่ม · golden 143 ตรง · verify เบราว์เซอร์จริง |
| 🚩 flag | ทำ guarded ได้ (ของใหญ่) จน verify |

### ธง 4 — 🔴 ผนังเบาใน G1 ลากฝ้าหลังคา + selector G3 มาด้วย (บั๊กจากการทำ 6 ปุ่ม · มติ 16 มิ.ย.)
- **อาการ (reproduce เบราว์เซอร์จริง):** กลุ่มบาน/กระจก → ติดตาย/ทึบ → ผนังเบา (ได้ wall_ext ถูก) **แต่ฟอร์มโผล่ selector แบบ G3** [หลังคา]/[ฝ้า-ผนัง] + รุ่นฝ้าหลังคา [ฝ้าอลู ตัวซี][ฝ้าไม้เทียม remood][ฝ้าฉาบเรียบ] → กดต่อเลือกฝ้าหลังคาได้ (ผิด) · "ผนังเบาโผล่ 2 ที่" = ชิป G1 + selector G3
| ช่อง | รายละเอียด |
|---|---|
| 🎯 จุด | จุด render family selector เมื่อ product cat='ฝ้า-ผนัง' (famSelectorHTML / G3 cascade) · เช็คเงื่อนไขเมื่อ `.i-group`='1' |
| 🔍 ของเดิม | cat 'ฝ้า-ผนัง' → render selector G3 (หลังคา/ฝ้า-ผนัง + ทุกรุ่นในหมวด รวม ceiling:1) เหมือนกันทุกกลุ่ม |
| ✏️ แก้เป็น | **เมื่อ group=1 + cat='ฝ้า-ผนัง'** → ไม่ render selector G3 · โชว์เฉพาะผนังเบา 3 ตัว (wall_ext/wall_int/isowall) แบบ **profile solid** (เลือกผนังนอก/ใน/isowall + ฉนวน +600 + ทาสี · ไม่มี หลังคา/ฝ้าหลังคา/กระจก/คาดตาราง) · กรอง `p.ceiling` ออก |
| ✅ เทส | `check-g1` → 🟢 "ผนังเบา G1 ไม่ลากฝ้าหลังคา" (probe เช็ค ตัวซี/ฉาบเรียบ/remood ต้องไม่โผล่) · golden 143 ตรง · verify เบราว์เซอร์ |
| 🚩 flag | - (แก้เงื่อนไข render) |
- **ข้อมูลถูกต้อง:** หมวด 'ฝ้า-ผนัง' (L639-661) มี 6 ตัว = ฝ้าหลังคา 3 (`ceiling:1`: ceil_cshape/ceil_bsc/ceiling_smooth) + ผนังเบา 3 (wall_ext/wall_int/isowall) · G1 เอาเฉพาะผนังเบา · ฝ้าหลังคาอยู่ G3 อย่างเดียว

## 🛡️ เทสก่อน-หลังทุกธง
```
node scripts/check-g1.mjs 2026-06-15     # เทียบดราฟ G1 (เป้า: 🔴=0)
node scripts/golden-snapshot.mjs          # กัน G1-G5,G7 ราคาเพี้ยน (engine ร่วม)
node test/g6-room-options.mjs             # solidlower/engine ร่วม G6 ไม่พัง
```
+ verify เบราว์เซอร์จริง (กดเลือก ykk_vent เห็นครอบวงกบ+ดรอปพื้น · บานทึบไม่มีแผ่นทึบล่าง · 6 ปุ่มกดได้)

## ⏭️ ลำดับแนะนำ
ธง 1 (ตัด solidlower · เล็ก) → ธง 2 (YKK · เล็ก) → ธง 3 (6 ปุ่ม · ใหญ่) · แต่ละธง commit + เทสเขียว
