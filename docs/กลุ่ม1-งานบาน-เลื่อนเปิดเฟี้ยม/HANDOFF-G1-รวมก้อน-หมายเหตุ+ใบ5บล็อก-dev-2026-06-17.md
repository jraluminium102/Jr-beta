# 📤 ใบสั่ง dev (G1 รวมก้อนเดียว · 5 งาน) → แชท B

> มติพี่นัท 17 มิ.ย. · รวมทุกงาน G1 ในไฟล์เดียว แก้ HTML รวดเดียว ([[consolidate-data-review]])
> **ติดตรงไหน / ไม่แน่ใจ → ถาม Chat A ก่อนแก้ อย่าเดา (พี่นัทสั่ง · กันแก้ผิด)**
> **กฎเหล็ก:** `node scripts/golden-snapshot.mjs` ก่อน-หลัง ต้องไม่เพี้ยน (ยกเว้นงาน 5 ที่แตะราคาโดยตั้งใจ) · verify เบราว์เซอร์จริง → ส่ง Chat A
> ลำดับ: **1 → 2 → 3 (ข้อความ/UI ปลอดภัย)** แล้วค่อย **4 → 5 (ราคา/สเปก · ระวัง)**

แหล่งข้อมูล: `docs/HANDOFF-ใบเสนอราคา-ข้อความ+สเปก-2026-06-17.md` (registry) · เป้าใบ: `docs/กลุ่ม1-งานบาน-เลื่อนเปิดเฟี้ยม/SAMPLE-G1-ใบ5บล็อก-2026-06-16.html`

---

# 1️⃣ หมายเหตุมาตรฐาน: ดรอปดาวน์ → ติ๊กหลายข้อ (ทุกกลุ่ม · UI)
แก้จุดเดียว `remarkSelectHTML()`+`addRemark()` (L1782-1786) · mount L3662 → G1-G7 ได้พร้อมกัน · ไม่แตะ oc-block/optbox · พับ default

**ก็อปวางทับ L1782-1786:**
```js
// 17 มิ.ย.: หมายเหตุมาตรฐาน = กล่องพับ + แถวติ๊กหลายข้อ (แทน dropdown) · ไหลลง .i-note เหมือนเดิม
function remarkSelectHTML(){
  var h='<details class="rk-block" style="margin-bottom:6px;border:1px solid var(--line);border-radius:6px;padding:4px 8px;">'
    +'<summary style="cursor:pointer;font-size:13px;color:var(--red);font-weight:600;">＋ เพิ่มหมายเหตุมาตรฐาน (ติ๊กได้หลายข้อ · ขึ้นในใบ)</summary>'
    +'<div class="rk-list" style="margin-top:6px;">';
  REMARK_PRESETS.forEach(function(grp){
    h+='<div style="font-size:12px;font-weight:700;color:var(--red-dark,#7f1d1d);margin:7px 2px 2px;">'+grp.g+'</div>';
    grp.items.forEach(function(t){
      var esc=t.replace(/"/g,'&quot;');
      h+='<label class="rk-item" style="display:flex;gap:8px;align-items:flex-start;padding:6px 6px;font-size:12.5px;cursor:pointer;border-radius:6px;">'
        +'<input type="checkbox" class="rk-cb" data-t="'+esc+'" onchange="rkToggle(this)" style="width:16px;height:16px;margin-top:2px;flex:none;">'
        +'<span>'+t+'</span></label>';
    });
  });
  return h+'</div></details>';
}
function rkToggle(cb){
  var ch=cb.closest('.ch'); var n=ch?ch.querySelector('.i-note'):null; if(!n)return;
  var t=cb.dataset.t, arr=n.value.split('\n'), idx=-1;
  for(var i=0;i<arr.length;i++){ if(arr[i].trim()===t){ idx=i; break; } }
  if(cb.checked){ if(idx<0){ var base=n.value.replace(/\s+$/,''); n.value=(base?base+'\n':'')+t; } }
  else if(idx>=0){ arr.splice(idx,1); n.value=arr.join('\n').replace(/\n{2,}/g,'\n').replace(/^\n+|\n+$/g,''); }
  if(typeof calcQuote==='function')calcQuote();
}
function rkSync(ch){
  if(!ch)return; var n=ch.querySelector('.i-note'); if(!n)return;
  var has={}; n.value.split('\n').forEach(function(s){ has[s.trim()]=1; });
  ch.querySelectorAll('.rk-cb').forEach(function(cb){ cb.checked=!!has[cb.dataset.t]; });
}
```
- เรียก `rkSync(ch)` หลัง addItem() · ลบ `addRemark` เดิมได้ (ไม่มี caller อื่น)

---

# 2️⃣ อัปเดตข้อความหมายเหตุ (registry ส่วน A · ทุกกลุ่ม · ข้อความ)
แก้ `REMARK_PRESETS` (L1760-1781) → **ก็อปวางทับทั้งก้อนด้วยลิสต์นี้** (🟡 ขยายข้อความ + 🔴 เพิ่มใหม่ · "นั่งร้าน" เก็บ 2 แถว ไม่ยุบ):
```js
const REMARK_PRESETS=[
 {g:'รวมในราคา',items:[
   'ราคาที่เสนอรวมงานรื้อประตูเดิม ในกรณีที่เกิดความเสียหายจากการทำงาน เช่น วัสดุเดิมเป็นรอย กระจกแตก งานเก็บสี จะมีบริการเปลี่ยนใหม่และเก็บงานให้โดยไม่มีค่าใช้จ่าย แต่ไม่รวมวัสดุ (ลูกค้าดำเนินการเตรียมวัสดุให้)',
   'ราคาที่เสนอรวมงานดรอปพื้น ในกรณีที่เกิดความเสียหายจากการทำงาน เช่น กระเบื้องพื้นแตกร้าว งานเก็บสี จะมีบริการเปลี่ยนใหม่และเก็บงานให้โดยไม่มีค่าใช้จ่าย แต่ไม่รวมวัสดุ (ลูกค้าดำเนินการเตรียมวัสดุให้)',
   'ราคาที่เสนอรวมงานกรีดพื้นเพื่อฝังรางยู ในกรณีที่เกิดความเสียหายจากการทำงาน เช่น กระเบื้องพื้นแตกร้าว งานเก็บสี จะมีบริการเปลี่ยนใหม่และเก็บงานให้โดยไม่มีค่าใช้จ่าย แต่ไม่รวมวัสดุ (ลูกค้าดำเนินการเตรียมวัสดุให้)',
   'ราคาที่เสนอรวมงาน Protection เฉพาะบริเวณแนวติดตั้ง (ไม่รวมเฟอร์นิเจอร์)',
   'ราคาที่เสนอรวมค่าตั้งนั่งร้านเหล็กเพื่อขนของขึ้นพร้อมติดตั้ง',
   'ราคาที่เสนอรวมงานรื้อราวกันตก ในกรณีที่เกิดความเสียหายจากการทำงาน เช่น งานเก็บปูนแตก งานเก็บสี จะมีบริการเปลี่ยนใหม่และเก็บงานให้โดยไม่มีค่าใช้จ่าย แต่ไม่รวมวัสดุ (ลูกค้าดำเนินการเตรียมวัสดุให้)',
   'ราคาที่เสนอรวมงานรื้อหลังคาเดิม ในกรณีที่เกิดความเสียหายจากการทำงาน เช่น โครงเดิมเป็นรอย วัสดุมุงหลังคาเดิมแตกร้าว งานเก็บสี จะมีบริการเปลี่ยนใหม่และเก็บงานให้โดยไม่มีค่าใช้จ่าย แต่ไม่รวมวัสดุ (ลูกค้าดำเนินการเตรียมวัสดุให้)',
   'ราคาที่เสนอรวมงานกรีดฝ้าเพื่อซ่อนคานซ่อนราง ในกรณีที่เกิดความเสียหายจากการทำงาน เช่น วัสดุเดิมเป็นรอย งานเก็บสี จะมีบริการเปลี่ยนใหม่และเก็บงานให้โดยไม่มีค่าใช้จ่าย แต่ไม่รวมวัสดุ (ลูกค้าดำเนินการเตรียมวัสดุให้)',
   'ราคาที่เสนอรวมงานทาสีจริงผนังเบากรุสมาร์ทบอร์ด (ลูกค้าดำเนินการจัดเตรียมสีให้บริษัท)',
   'ราคาที่เสนอรวมงานเดินไฟดาวน์ไลท์แบบท่อลอย .. ดวง (ไม่รวมชุดดวงโคม ลูกค้าดำเนินการจัดเตรียมชุดดวงโคม) ใช้สวิตช์ไฟเดิม / พร้อมสวิตช์ไฟ .. จุด และปลั๊กไฟ .. จุด และไม่รวมงานเดินไฟเข้าตู้เมนหลัก',
   'ราคาที่เสนอรวมงานนั่งร้าน และโพรเทคฟิล์มฟ้าบริเวณกระจกเพื่อกันสิ่งสกปรก แต่ไม่รวมงานทำความสะอาด Big cleaning',
   'ราคาที่เสนอรวมโครงยึดเข้าตัวบ้านพร้อมครอบอลูมิเนียม' ]},
 {g:'ไม่รวมในราคา',items:[
   'ราคาที่เสนอไม่รวมงานดรอปพื้น',
   'ราคาที่เสนอไม่รวมวัสดุ (ลูกค้าดำเนินการเตรียมวัสดุให้)',
   'ราคาที่เสนอไม่รวมเจาะเสาบานสำหรับใส่มือจับ Digital Door Lock และงานติดตั้งมือจับ Digital Door Lock (ลูกค้าดำเนินการเอง)',
   'ราคาที่เสนอไม่รวมงานรื้อบานประตูเดิมและทุบปูนเดิม (ลูกค้าดำเนินการเอง)',
   'ราคาที่เสนอไม่รวมงาน ลงเข็ม / เทพื้น / ปูกระเบื้อง / งานไฟ / งานก่อผนังปูน / งานดรอปพื้น',
   'ราคาที่เสนอไม่รวมงาน รื้อพื้นเดิม / รื้อกระเบื้อง / รื้อหลังคาเดิม / งานเดินไฟ / งานสี',
   'ราคาที่เสนอไม่รวมงาน ย้ายกล้องวงจรปิด / ย้ายแอร์ (ลูกค้าดำเนินการเอง)',
   'ราคาที่เสนอไม่รวมงาน ถอดรางน้ำเดิม / งานทาสี / งานเดินไฟ',
   'งานเดินไฟสำหรับมอเตอร์ (พ่วงปลั๊กบริเวณใกล้เคียง ระยะเดินสายไฟมอเตอร์ไม่เกิน 10 เมตร) และไม่รวมงานเดินไฟเข้าตู้เมนหลัก' ]},
 {g:'เงื่อนไข / วัสดุ',items:[
   'วัสดุและอุปกรณ์ตามแบบมาตรฐานผู้ผลิต',
   'จำนวนและราคาอาจมีการเปลี่ยนแปลงเมื่อช่างเข้าวัดหน้างานจริง',
   'วัสดุ Sealant ภายนอก-ภายใน ใช้ PU / MS สีขาว (สำหรับทาสีได้), ใช้ซิลิโคนใสสำหรับยิงกระจก',
   'ราคาที่เสนอยังไม่รวมภาษีมูลค่าเพิ่ม' ]},
];
```
> งาน 2 เป็นแค่ data list (ไม่กระทบ calc) · ทำคู่กับงาน 1 ได้เลย

---

# 3️⃣ เก็บหัวใบ 5 บล็อก G1 ให้ตรง sample (ข้อความ)
**โครงสร้าง 5 บล็อกทำงานแล้ว** (`itemDetail` คืน work/spec L5695 · qrow render ครบ) — เหลือเก็บ "หัว" 2 จุด (พี่นัทเคาะ):

**ปัญหาจริงตอนนี้ (จาก dump):** หัว = `ประตูบานเปิด (รุ่น L ยูโร) พร้อมมุ้งเฟรมใหญ่ บานเปิด ขนาด 1.2×2.2 ม. (1.2 × 2.2 ม.)`
→ มุ้งโผล่ในหัว(ทั้งที่อยู่ในรายการแล้ว) + ขนาดซ้ำ 2 รอบ + บานหลักไม่ขึ้นรายการ

**แก้ 2 จุด:**
1. **เอา "มุ้ง + ขนาด" ออกจาก `prodName`** เมื่อเป็น G1 5บล็อก (`_g1FB`) — จุดที่เติม: L5176-5184 (`prodName += ' '+_mosqNm+_mqDim`) สำหรับบานทั่วไป · บานเลื่อน = `_mosqStr` ใน `_slideName` (L5155-5168)
   - เพราะ มุ้งอยู่ใน work แล้ว · ขนาด qrow ใส่ผ่าน `_qSz` ครั้งเดียว (L5790-5793)
2. **เพิ่มบานหลักเป็นบรรทัดแรกของ `_g1Work`** เช่น `บานเปิด` / `บานเลื่อนรางล่าง` / `กระจกติดตาย` (จาก it.p.cat / rawName · ตัดคำ "ประตู/หน้าต่าง" นำออก)
   - ผลลัพธ์ที่ต้องการ (เคส 1): หัว=`ประตูบานเปิด (รุ่น L ยูโร)` · รายการ=`- บานเปิด` / `- มุ้งเฟรมใหญ่ (บานเปิด)` · รายละเอียด=สี/กระจก/ผ้ามุ้ง

> ⚠ จุดนี้ซับซ้อน (โค้ดชื่อซ้อนหลายชั้น) — **ถ้าไม่ชัวร์ว่าตัดตรงไหนไม่ให้กระทบบานเลื่อน/กลุ่มอื่น ถาม Chat A ก่อน** · เทียบผล dump กับ sample ทุกชนิดบาน

---

# 4️⃣ ผนังเบา — สเปกเต็ม + แก้ "ภายใน=ยิปซั่ม" (registry #8)
`wall_ext`/`wall_int` = L699-700 · ตอนนี้ใบเขียน "สมาร์ทบอร์ด" ทั้งนอก-ใน (ผิด) → พิมพ์สเปกลงใบให้ครบ:
- **ภายนอก (wall_ext):** "ผนังสมาร์ทบอร์ดพร้อมฉนวนกันร้อนกันเสียง โครงเหล็กชุบซิงค์(ไร้สนิม) 1.2 มม. พร้อมโครงคร่าวซี-ไลน์เสริมแรง"
- **ภายใน (wall_int):** "ผนังยิปซั่มพร้อมฉนวนกันร้อนกันเสียง โครงคร่าวซี-ไลน์เสริมแรง"  ← **ยิปซั่ม ไม่ใช่สมาร์ทบอร์ด**
> ข้อความลงใบล้วน ไม่แตะราคา · จุดพิมพ์สเปกผนัง = ใน itemDetail/det ของ wall_* (ถ้าหาจุดไม่เจอ ถาม Chat A)

---

# 5️⃣ ธรณีบานเปิด → 4 ตัวเลือก (registry #9 · ⚠ แตะราคา)
`o-thresh` (L3850) ตอนนี้มี 2 ตัว: `std` กันน้ำ(+0) · `turtle` หลังเต่า+Drop Seal(+1,000 · ราคาที่ L1603-1606)
**เพิ่มอีก 2 ตัว (เฉพาะบานเปิด · ฟรีทั้งคู่ +0):**
- `turtle_felt` = "ธรณีหลังเต่า + สักหลาดด้านล่าง" (+0)
- `none` = "ไม่มีธรณี" (+0)
แก้: (ก) เพิ่ม `<option>` + ชิป `rfChips('o-thresh',...)` ที่ L3850 ครบ 4 ตัว · (ข) calc L1603 ชาร์จ +1,000 **เฉพาะ `turtle` เท่านั้น** (turtle_felt/none = +0) · (ค) พิมพ์ลงใบตามชื่อที่เลือก
> ⚠ แตะ calc → golden จะขยับเฉพาะเคสเลือกธรณีใหม่ (default `std` ต้องไม่เพี้ยน) · ถ้าไม่ชัวร์ logic ราคา ถาม Chat A

---

## ✅ เทสรวม (ก่อนปิด)
- `node scripts/golden-snapshot.mjs` → งาน 1-4 ต้อง 0 เพี้ยน · งาน 5 เพี้ยนเฉพาะเคสธรณีใหม่ (อธิบายได้)
- `node scripts/check-g1.mjs 2026-06-17`
- **เบราว์เซอร์จริง:**
  - หมายเหตุ: ขยาย→ติ๊กหลายข้อ→ขึ้น .i-note→genQuote ขึ้นใบ→ติ๊กออกหาย · เห็นข้อความใหม่ (Protection/นั่งร้านเหล็ก/มอเตอร์)
  - ใบ G1: หัวสั้น ขนาดครั้งเดียว · บานหลัก+มุ้งอยู่รายการ · OPTION/หมายเหตุ ว่าง=ซ่อน · สีพิเศษ ⚠
  - ผนังเบาภายใน = "ยิปซั่ม" · ธรณีบานเปิด 4 ตัวเลือก (turtle_felt/none = ราคาไม่เปลี่ยน)
  - → ส่ง Chat A verify

## กันบานปลาย
- ทำ G1 + หมายเหตุ(ทุกกลุ่ม) เท่านั้น · G อื่น (ใบ 5บล็อก/ข้อความ) ไล่ตามทีหลัง ([[one-group-at-a-time]])
- "ออปชั่นทั้งใบ" ไว้ท้ายใบเหมือนเดิม · ไม่แตะ VAT/ส่วนลด
- **ติด/สงสัยจุดไหน ถาม Chat A ก่อนแก้** (พี่นัทเน้น)
