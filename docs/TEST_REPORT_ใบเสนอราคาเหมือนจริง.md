# Test Report — ปรับใบเสนอราคาให้เหมือนใบจริง 100%

วันที่: 2026-06-04 · เทียบกับใบจริงใน `07_ต้นฉบับทุน_อ้างอิง/ตัวอย่างใบเสนอราคา/`
อ้างอิงหลัก: `QT2025100030_คุณขวัญ` (มีส่วนลด) + `QT2026030093_คุณปีเตอร์` (4 หน้า + หน้าเงื่อนไขเต็ม)
วิธี: แก้ `genQuote` ตัวจริงในเครื่องคิดราคา → spawn subagent QA 2 ตัว crosscheck PDF ที่ออก เทียบใบจริง

> 🟢 แก้แล้ว/ตรง · 🟡 เหลือ (ข้อเสนอแนะ) · 🔴 ต้องแก้ (แก้แล้วทั้งหมด)

---

## 1. จุดที่พี่นัทสั่งแก้ — แก้ครบทุกข้อ
| # | สั่งแก้ | สถานะ | รายละเอียด |
|---|--------|--------|-----------|
| 1 | **ตัดราคาทิ้งจากรายละเอียด ไม่ต้องโชว์** | 🟢 | ตัด `it.r.msgs`/`addonLabel` (เช่น "× 9,000", "+14,000", "= 20,160", "ปลีก ×2.0") ออกจาก "รายละเอียดงาน" — เหลือเฉพาะวัสดุ |
| 2 | **ข้อมูลหลักอยู่ช่องบน ไม่ใช่ดีเทลวัสดุ** | 🟢 | หัวข้อหลัก (ตัวหนา) = ชื่องาน/พื้นที่ (จาก `it.position`) · ใต้ลงมา "- ชื่อสินค้า" · วัสดุย้ายไปใต้ "รายละเอียดงาน" |
| 3 | **เงื่อนไขต้องเหมือนใบจริงเดิมโดยสมบูรณ์** | 🟢 | แทนข้อความเงื่อนไขด้วยของจริงเป๊ะ: เงื่อนไขการเข้าทำงาน 5 ข้อ (+ 2.1-2.3) + เงื่อนไขแบบและใบเสนอราคา 6 ข้อ (+ 2.1-2.3) + "ขอยืนยันการสั่งซื้อภายใต้เงื่อนไข ขอแสดงความนับถือ" + ช่องลงนาม |

## 2. จุดเพิ่มเติมที่ crosscheck เจอ แล้วแก้ให้ตรงใบจริง
| # | ใบจริง | เดิม (ผิด) | แก้เป็น | สถานะ |
|---|--------|-----------|---------|--------|
| 4 | ไม่โชว์ "ขนาด" ในใบ | มี "ขนาด: 3.6×2.4 ม." | ตัดบรรทัดขนาดออก | 🟢 |
| 5 | หัวคอลัมน์ "ราคาต่อหน่วย" | "ราคา/หน่วย" | "ราคาต่อหน่วย" | 🟢 |
| 6 | ยอดท้ายบิล | "ราคารวม / ราคาสินค้าสุทธิ / VAT 7% / รวมทั้งสิ้น" | **"รวมเป็นเงิน / (ส่วนลด / จำนวนเงินหลังหักส่วนลด) / ภาษีมูลค่าเพิ่ม X% / จำนวนเงินรวมทั้งสิ้น"** | 🟢 |
| 7 | ตัวเลข "47,000.00" (ไม่มี ฿) + "X.XX บาท" | "฿47,000" (ไม่มีทศนิยม) | ทศนิยม 2 ตำแหน่ง + ลงท้าย "บาท" ในยอดรวม | 🟢 |
| 8 | บรรทัดตัวอักษร "(...บาทถ้วน)" เดี่ยวๆ | มี "— จำนวนเงินรวมทั้งสิ้น ฿..." ต่อท้ายเกิน | เหลือ "(...บาทถ้วน)" อย่างเดียว | 🟢 |
| 9 | ไม่บวก Protection อัตโนมัติ | บวก "Protection พื้น/ผนัง ฿2,000" | ปิด svc-protect ในการออกใบ | 🟢 |

## 3. ตรวจรายการ (subagent QA #2 — เทียบ PDF ทุก row)
- **quote-01 (สินค้าราคาคงที่ 6 รายการ): ผ่านทุกข้อ** — หัวข้อ=ชื่อพื้นที่, ไม่มีขนาด, ไม่มีราคาย่อย, วัสดุครบ, จัดเรียงถูก
- **quote-05 (กั้นห้องกระจก/ม่านซิป/มุ้ง/ราว): HTML สะอาดแล้ว** (ตรวจ `quote-05.html` ยืนยัน: 0 "จุดที่", 0 "ขนาด:", 0 ราคาย่อย) — รอบที่ subagent เห็นปัญหาเป็น PDF ค้าง (ไฟล์ถูก viewer ล็อก ยังไม่ทับด้วยตัวใหม่)

## 4. crosscheck ระดับเอกสาร (subagent QA #1)
- 🟢 หัวคอลัมน์ตรง · ยอดรวม wording ตรง · บล็อกลายเซ็นตรง · หน้าเงื่อนไขตรงคำทุกข้อ

## 5. เทสอัตโนมัติ (ยืนยันไม่ regress หลังแก้ format)
อัปเดต parser เทสให้รองรับเลขทศนิยม + label ใหม่ (เหมือน format ใบจริง):
| ไฟล์ | ผล |
|------|----|
| calc-smoke / calc-functional / calc-glasshouse | 11/11 · 21/21 · 6/6 |
| qa-t1..t4 (บานเลื่อน/กระทุ้ง/ติดตาย/มุ้ง-ราว) | 4/4 ทุกตัว |
| qa-t5-totals (ยอดรวม/VAT/ส่วนลด/งวด) | 18/18 |
| qa-options / qa-t6-zipscreen | 12/12 · 6/6 |
| **รวม** | **90/90 · ไม่มี JS error** |

---

## 6. รอบที่ 2 (2026-06-04 บ่าย) — ลุยจุดที่เหลือ (ยกเว้นโลโก้)
| # | จุด | สถานะ | รายละเอียด |
|---|-----|--------|-----------|
| 1 | หัวบิลขวา ผู้ขาย/ผู้ติดต่อ/เบอร์โทร | 🟢 **เสร็จ** | เพิ่มช่องกรอกในเครื่องคิดราคา + render ในกล่องหัวขวา (เลขที่/วันที่/ผู้ขาย · ผู้ติดต่อ/เบอร์โทร) |
| 2 | ที่อยู่ลูกค้าเต็ม | 🟢 **เสร็จ** | เพิ่มช่อง "ที่อยู่ลูกค้า" + render ใต้บล็อก "ลูกค้า" (ชื่อ + ที่อยู่) |
| 3 | ข้อมูลบริษัทซ้ำทุกหน้า | 🟢 **เสร็จ** | running footer (`position:fixed` print) โชว์ บจก./เลขภาษี/โทร/เลขที่ใบ **ทุกหน้า** (ตรวจ PDF หน้า 1-3 มีครบ) |
| 3b | เลขหน้า "หน้าที่ X/Y" | 🟡 ข้อจำกัด | Edge `--print-to-pdf` (CLI) ไม่รองรับ page-counter ในเนื้อหา · ต้องใช้ DevTools footerTemplate หรือ PDF engine อื่น (Prince/wkhtmltopdf) — ข้อมูลบริษัทซ้ำได้แล้ว ขาดแค่ตัวเลขหน้า |
| 4 | โลโก้ | ⏭️ **ข้าม** (ตามที่สั่ง) | คงโลโก้ "JR." เดิม |
| 5 | OPTION/หมายเหตุ ต่อรายการ | 🟢 **เสร็จ** | เพิ่มช่อง "หมายเหตุ / OPTION (ขึ้นในใบ)" ต่อรายการในเครื่องคิดราคา → พิมพ์หลายบรรทัดได้ · render ระหว่าง "- คำอธิบาย" กับ "รายละเอียดงาน" (บรรทัดขึ้นต้น OPTION=แดง, อื่นๆ=เทา) ตรงโครงสร้างใบจริง · ตัวเลข OPTION เป็นค่าที่เซลล์กรอกเอง (กันเลขผิดจาก auto) |

## 7. ข้อเสนอแนะคงเหลือ
1. **เลขหน้า X/Y**: ถ้าต้องการ ให้สลับการแปลง PDF ไปใช้ headless DevTools (`Page.printToPDF` + footerTemplate `<span class="pageNumber"></span>/<span class="totalPages"></span>`) แทน Edge CLI
2. **OPTION/คำบรรยายละเอียด (ข้อ 5)**: เพิ่ม field free-text ต่อรายการ (งานเล็ก) เพื่อให้พิมพ์ OPTION/หมายเหตุได้เอง — ผมทำต่อได้ถ้าต้องการ
3. ใบทดสอบ 10 ใบ (HTML+PDF) อยู่ใน `ใบเสนอราคาทดสอบ/` · ถ้า PDF บางใบยังเก่าให้ปิด viewer แล้วรัน `node scripts/gen-quotes.mjs` + แปลง PDF ใหม่

## 7. สรุป
- **โครงสร้างใบ + ราคาสินค้า + เงื่อนไข = ตรงใบจริงแล้ว** (item format, totals wording, ตัวเลข, เงื่อนไขคำต่อคำ)
- เหลือเฉพาะ **ฟิลด์หัวบิล (ผู้ขาย/ผู้ติดต่อ/ที่อยู่) + การซ้ำหัวทุกหน้า + โลโก้** ซึ่งต้องเพิ่ม data/feature — ไม่ใช่เรื่อง format ที่แก้ใน genQuote ได้จุดเดียว
- ไฟล์ทดสอบ 10 ใบ (HTML+PDF) อยู่ใน `ใบเสนอราคาทดสอบ/` · เปิด `index.html` ดูรวมได้
- ⚠️ ถ้า PDF บางใบ (03/04/05) ยังเป็นของเก่า ให้ปิดโปรแกรมที่เปิดไฟล์ค้างไว้ แล้วรัน `node scripts/gen-quotes.mjs` + แปลง PDF ใหม่
