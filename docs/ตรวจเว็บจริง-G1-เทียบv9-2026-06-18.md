# 🌐 ผลตรวจเว็บจริง (live) G1 เทียบเป้าหมาย v9 — 18 มิ.ย. 2026

> ตรวจผ่าน Claude in Chrome บนเว็บจริง **jr-beta-azure.vercel.app/calculator** (iframe โหลด `/calculator/index.html` · R3.9.2)
> วิธี: render ฟอร์มจริงในเบราว์เซอร์ + อ่าน DOM/JS จากโค้ดที่รันจริง (ไม่เดา)
> **สรุปใหญ่: Live ตรงกับ repo ที่เราแก้เป๊ะ** (ไม่มี deploy ค้าง) · ความต่างจาก v9 = งานที่ยังอยู่ในคิว รอ Chat B

---

## ✅ ทำเสร็จ + ขึ้นเว็บแล้ว (2 จุด)

### 1. รวมชุดบาน G1+G6 — DONE & live ✅
- `FAM_CATS['1']` = **14 ชนิด** (รวม ฝ้า-ผนัง · บานเปลือย · บานหมุน · shower แล้ว)
- `FAM_CATS['6']` = **12 ชนิด** (รวม YKK · ดัดโค้ง แล้ว)
- = มติ 18มิ.ย. (รอบ3 ข้อ5) dev ทำเสร็จ+deploy แล้วระหว่างที่เราคุย → **ตัดออกจากคิวได้**

### 2. ลักษณะการเปิดบานเลื่อน (opentype) — มีจริง ✅
- บานเลื่อน render `.o-opentype` ("ชนิดการเปิด") ลำดับที่ 3 · บานเปิด render `.o-thresh` (ธรณี) ลำดับ 3
- (พี่ห่วงว่าหายตอน v8 — ของจริงไม่หาย ครบ)

---

## ❌ ยังไม่ทำ — ต่างจาก v9 (3 จุด · รอ Chat B)

### 3. แผ่นทึบล่าง (ลูกฟูก/คอมโพสิต) — ยังไม่มี ❌
- เทสจริง: `.o-solidlower` **ไม่ render** ทั้งบานเลื่อน/บานเปิด (ตรงกับที่ตัดทิ้ง 15มิ.ย.)
- = งานที่เพิ่งเคาะ 18มิ.ย. "เอากลับ + ล็อกเฉพาะบานกระจกหลัก" → ยังรอทำ (logic ราคา dormant พร้อม L1792)

### 4. มือจับ → ล็อค — ยังไม่แปลง ❌
- เทสจริง บานเลื่อน: ยังมี **"มือจับ ติ๊กแบรนด์ที่ใช้"** (ลำดับ 9) + "ชุดล็อค" (ลำดับ 10) อยู่คู่กัน
- v9 = เหลือ "ล็อค" อย่างเดียว · มือจับละเอียดย้ายไปกล่อง OPTION → ยังรอทำ

### 5. จัดลำดับ v9 (ของไม่บ่อยพับ · OPTION โชว์เลย · ธรณีต่อจากกระจก) — ยังไม่จัด ❌
- เทสจริง ลำดับออปชั่นบานเลื่อนตอนนี้: รุ่น → ชนิดการเปิด → ราง → **ฝังรางยู → ครอบวงกบ → ดรอปพื้น** (ของไม่บ่อยโผล่กลางฟอร์ม) → มือจับ → ล็อค → คาดตาราง → มุ้ง
- มี `<details>` 6 อัน (OPTION ลูกค้ายังพับ) · v9 = ของไม่บ่อยพับลงล่าง · OPTION โชว์เลย · ธรณีขึ้นต่อจากกระจก → ยังรอทำ

---

## สรุปตาราง

| # | เป้าหมาย v9 | Live ตอนนี้ | สถานะ |
|---|---|---|---|
| 1 | รวมชุดบาน G1+G6 | FAM 14/12 ครบ | ✅ เสร็จ+live |
| 2 | opentype บานเลื่อน | มี (ลำดับ 3) | ✅ เสร็จ |
| 3 | แผ่นทึบล่าง ลูกฟูก/คอมโพสิต | ไม่ render | ❌ รอ (เพิ่งเคาะ) |
| 4 | มือจับ→ล็อค | มือจับ+ล็อค ยังคู่ | ❌ รอ |
| 5 | ลำดับ v9 (พับ/OPTION โชว์/ธรณีบน) | ยังเดิม | ❌ รอ |

> Live = repo sync กัน (ไม่มี deploy lag) · ความต่าง = คิวที่ยังไม่ถึง Chat B · ไม่พบบั๊ก/regression

---

## 🔁 Prompt ตรวจงานเว็บจริงซ้ำ (กอปใช้ได้ทุกรอบ)

> เปิด `jr-beta-azure.vercel.app/calculator` แท็บ "ใบเสนอ" → ผ่าน Claude in Chrome รัน JS ใน iframe (`document.querySelector('iframe').contentWindow`):
> 1. addItem → set `.i-group`=1, `.i-prod`=sliding_sms (บานเลื่อน) / casement_euro (บานเปิด) → fire change
> 2. เช็ค control: `.o-opentype` `.o-thresh` `.o-solidlower` `.o-mosq` `.o-gridmark` `.o-slidelock` มี/ไม่มี
> 3. ดึงลำดับ `label.opt` → เทียบ v9 (ของไม่บ่อยควรพับล่าง · ธรณีต่อจากกระจก)
> 4. เช็ค `FAM_CATS['1']/['6']` ครบ 14/12 · `<details>` (OPTION พับ?)
> 5. เทียบกับ repo: `node scripts/_probe-g1-realstate.mjs` → ต้องตรงกัน (ถ้าต่าง = deploy ค้าง)
