# 🚀 คำสั่งรันยาว — แก้ 4 ก้อนรวดเดียว (master) · 25 มิ.ย.

> พอ index ว่าง พี่พิมพ์ทริกเกอร์ → ผมรันตามนี้ทั้งหมด · ทุกขั้น golden 150/150 + commit ต่อก้อน
> **ทริกเกอร์:** "index ว่างแล้ว ลุยตาม MASTER-RUN-ORDER"

---

## ก่อนเริ่ม (ครั้งเดียว)
```
git pull --rebase origin main
node scripts/golden-snapshot.mjs   # baseline ต้อง 150/150 (ถ้าไม่ผ่าน หยุด แจ้งก่อน)
```

## ลำดับรัน (เสี่ยงน้อย→มาก · commit+push ทุกก้อน)

### ก้อน A — ใบเสนอราคา ② print (เป๊ะสุด ทำก่อน)
- patch CSS print (เลิก absolute+visibility → display:none+static) · ดู `ORDER-dev-ใบเสนอราคา-แก้`
- เทสพิมพ์ใบยาว >1 หน้า → ไหลครบ · golden 150/150 · commit "fix(print): ใบไหลครบทุกหน้า"

### ก้อน B — โหมดคิดเร็ว G1-G7 (ดราฟ frozen)
- ตาม `ORDER-dev-quickmode-G2G3-align-G1` ส่วนที่ 0-8 · ลำดับ G1 มุ้ง→G2(สี+มุ้งติดตาย+ทำเป็นบาน)→G3 สีโครง dropdown→G5(สีกรอบ)→G4(กระจกตู้)→G6(5สเตป สีเดียว)
- golden ทุก G · commit ต่อ G

### ก้อน C — G2 ระแนง (โหมดเต็ม)
- ① เฟี้ยมทำเป็นบาน เพิ่มเปิดซ้าย/ขวา N บาน (กอป G1) · ② กล่องสีเติมครบ+ตัดสีชุบ+ชื่อพิเศษ · ③ กล่องสีประตูรั้ว=เหมือนระแนง · ดู `review-g2-fix`
- golden · commit "fix(G2): เฟี้ยมN บาน + สีครบ + ประตูรั้วสีเหมือนระแนง"

### ก้อน D — G3 หลังคา (โหมดเต็ม + ใบ)
- ② ตัดสีชุบ · ③ ยาว×ยื่นหลายช่วง+รวม ตร.ม. · ①④⑤ จัด layout เหมือนดราฟ · ⑥ ใบ สีโครงก่อนหลังคา (L7106-7113) · ดู `review-g3-fix`
- golden · commit "fix(G3): ตัดสีชุบ+หลายขยัก+layout+ใบสลับสี"

### ก้อน E — ใบเสนอราคา ① หัวห้อง + ④ วิธีคิดราคา
- ① หัวห้อง G6 "ห้องต่อเติม(กั้นห้องกระจกพร้อมหลังคา)" + ตัดราคาย่อยใบปกติ · ④ qCalcStepLines addonLines
- golden · commit

## หลังรันครบ
```
node scripts/golden-snapshot.mjs        # 150/150
node scripts/dump-controls.mjs g2 / g3  # โครงตรงดราฟ
```
- เทสเว็บสด + **ทำใบเทสจริง + ส่งภาพจริง** (Claude-in-Chrome แคป · screenshot timeout)
- ส่งเช็คลิสต์: `review/quote-fix-checklist` + เช็คลิสต์ G2/G3

---
## ⚠️ กฎระหว่างรัน
- ห้ามเพิ่มมติใหม่กลางทาง (เจอ→จดรอบหน้า)
- golden ไม่ผ่าน = หยุดทันที แจ้งก่อน push
- 1 แชทเดียวรัน (ไม่มีแชทอื่นแตะ index พร้อมกัน)
