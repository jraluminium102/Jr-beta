# HANDOFF — Chat B ก้อนรวม (9 ข้อ · เรียงลำดับ) · 17 มิ.ย. 2026

> ทำ **ทีละข้อ + golden-snapshot ก่อน-หลัง + verify browser** · เสร็จกลุ่มแล้วหยุดให้ตรวจ
> 🔒 ไม่แตะราคาตัวอื่น (golden 149 ต้องนิ่ง ยกเว้นข้อ 1-2 ที่ตั้งใจให้ราคาขึ้น)

## 🔴 รีบสุด — บั๊กการเงิน (undercharge · ราคาจะเพิ่ม = ตั้งใจ)
**1. G5 สีกรอบมุ้งเฟรม ไม่คิดเงิน** — engine `screen_addon` L1164-1193 return ก่อนโค้ด mscolor → imp21/22/23 ไม่คิดสีกรอบ
→ คิด colorPrice + push สีกรอบลง `_mSpec` (ขึ้นใบ) · เทียบ non-screen (imp28) ที่คิด +1,000 ปกติ

**2. G5 ครอบวงกบมุ้งเฟรม ไม่คิดเงิน** — return L1193 ก่อนถึงครอบวงกบ L1578 · control o-fcsides โผล่แต่ Δ0
→ ย้าย/เรียกโค้ดครอบวงกบ ก่อน return

## 🟡 ข้อความ/สเปก
**3. G5 ตัด "ห้าม PC"** จากชื่อ imp21 L765 → เหลือ "มุ้งเฟรมเล็ก ผ้ามุ้งไฟเบอร์ (บานเปิด/เลื่อน/กระทุ้ง/ยก)" (deny_with เก็บไว้ · คำเตือนภายในห้ามขึ้นใบ)
**4. G5 typo** imp28 L768 "รุ่ง"→"รุ่น" (มุ้งจีบ ตีนตะขาบ รุ่นรางเตี้ย)
**5. #8 ผนังเบา default** — `o-wallframe` default = "เหล็กชุบ" + เติม "1.2 มม." ในข้อความเหล็กชุบ (L5515) + `o-insul` default = ติ๊ก → เปิดมา default ตรงสเปกเฟิร์น (ลูกค้าเปลี่ยนได้)

## 🟠 redesign ปุ่ม (รอบใหม่)
**6. revert สี/กระจก "ทั้งใบ"** (`#global-color`/`#global-glass`) → เลิกครอบ drill-down กลับเป็น `<select>` ดรอปดาวน์ คงบนสุด · `.i-color`/`.i-glass` (รายบาน) คงทีละชั้น
**7. OPTION ทางเลือกลูกค้า** (`.oc-block`) → โชว์เลย (ไม่พับ) · ชิปประเภท [ไม่มี/เปลี่ยนกระจก/ผ้ามุ้ง/สี/มือจับ] → กด→ตัวเลือกโผล่→คิดราคาให้ (logic auto-price L4847 เดิม) · ดู `HANDOFF-redesign-ปุ่มใหญ่-drilldown` ข้อ ⑥
**8. drill-down อีก 2 จอ** — คิดเร็ว (`qi-color`/`qi-glass`) + กั้นห้อง (`gh-color-sel`/`gh-glass-sel`) ใช้ฟังก์ชัน drill เดิม hook เพิ่ม
**10. เปิดกลุ่มออปชั่นที่ใช้บ่อย ไม่พับ** — ใน `groupGHOpts` (L2854) กลุ่ม "🔧 มือจับ · ล็อค · มุ้ง · คาดตาราง" เปลี่ยน `open:false`→**`open:true`** · "➕ งานเสริม · หมายเหตุ" (L2866) **คง `open:false`** (ใช้น้อย) · ธรณี/ครอบวงกบ เปิดอยู่แล้ว · แค่เปลี่ยน flag ไม่แตะ logic/ราคา
**B3. ชินโคไลท์ → ชิปแบบ ค (ย่อ+จุดสี)** — chip โชว์ **ชื่ออังกฤษย่อ** (Modern Grey/Royal Blue) + จุดสี (palette ด้านล่าง) · ตัวที่เป็นไทย (ขาวขุ่น/ใส/เทาอ่อน) โชว์ไทย · **ชื่อเต็ม+รหัสยังลงใบ** (เช่น "HC-N828 Modern Grey")

**11. G2 ระแนง — ใส่คำ "ระยะห่าง" ในใบ** (พี่นัท 17มิ.ย. · จากภาพใบ) — ตอนนี้ใบโชว์ "...โชว์ 1" 5cm." แต่ไม่มีคำว่า "ระยะห่าง" → ใน itemDetail ระแนง (L~5285) ใส่ป้าย "ระยะห่าง" หน้าค่า เช่น "โชว์ 1" · ระยะห่าง 5 ซม." (ข้อความล้วน ไม่แตะราคา)

## ✅ B ทำต่อในไฟล์เดียวกัน (เดิมจะแยก G1 — พี่นัทสั่งทำให้จบเลย)
**9. ผ้ากันแมว/หมา → ผ้ากันแมว** L1713/5659/5771 (key anti_pet · G1 มุ้งติดบาน) — แค่แทนคำ ไม่เสี่ยง · ⚠ **แจ้งแชท G1 ให้รู้ว่า wording นี้ B แก้ให้แล้ว** (กันทำซ้ำ)

---

## Palette ชินโคไลท์ (B3 · เสนอจากชื่อสี · พี่นัทปรับ hex ได้)
| รหัส | ชื่อ | จุดสี (เสนอ) |
|------|------|-------------|
| HC-N828 | Modern Grey | `#8a8d8f` |
| HC-B703 | Royal Blue | `#26408b` |
| HC-570 | Classic Brown | `#6b4a2f` |
| HC-N590 | Noble Green | `#2f5d43` |
| SP-NB00 | ขาวขุ่น | `#efece4` (มีขอบ) |
| SP-NB30 | Foggy Brown | `#8a7a68` |
| SP-B857 | Marine Blue (ฟ้าน้ำทะเลฝ้า) | `#3a6e88` |
| SP-NC95 | Light Brown | `#a98b63` |
| NT-001 | ใส | `#e9f1f1` (มีขอบ · โปร่ง) |
| NT-332 | เทาอ่อน | `#c2c4c2` |
| Prime 562 | Brownish Green | `#5a6347` |
| Shade-430S | Pearl White | `#f4f2ec` (มีขอบ) |
