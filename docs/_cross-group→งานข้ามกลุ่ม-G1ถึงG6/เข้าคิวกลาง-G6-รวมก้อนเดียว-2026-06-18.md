# 📦 [เข้าคิวกลาง · จากแชท A-G6] รวมก้อนเดียว — ส่งคิวกลางจัดการต่อทั้งหมด
**18 มิ.ย. 2026** · รวม 3 ใบคิวเดิม + ดราฟ UX ใหม่ → ก้อนเดียวจบ · เรียงตามความสำคัญ
**⛔ แชท G6 READ-ONLY · ไม่แก้เอง · ไม่ส่ง Chat B ตรง — คิวกลางจัดลำดับ/รวมกับงานระบบ แล้วส่ง B รอบเดียว**

> 🔴🔴 **NEW 18มิ.ย. (เจอตอนเทสออกใบจริง 5 ใบบนเครื่องคิดราคา · เทสสด localhost=vercis):**
> - **E1 (บั๊กการเงิน · สำคัญสุด · ROOT CAUSE เจอแล้วจากโค้ด):** **VAT ไม่ได้คิดผิด — เป็นบั๊กแสดงผล: Protection 2,000 ถูกซ่อน (charge จริงแต่ไม่โชว์)**
>   - กลไก (ยืนยันโค้ด): `jobServices` L5044-5047 → svc-protect (default ติ๊ก) `_hidden += 2000` + push **หมายเหตุ** (ไม่ push บรรทัดราคา) · L5076 `return {amt: lines.sum + _hidden}` → amt รวม 2,000 แต่ `lines` ว่าง
>   - `buildQuoteDoc`: L5322 **"รวมเป็นเงิน" = subtotal (20,000) ไม่รวม svcAmt** · L5325 `svcLines.map` = ว่าง (ไม่มีบรรทัด Protection) · L5301 **`vat=(net+svcAmt)*7%` = 22,000×7% = 1,540** · grand = net+svcAmt+vat = 23,540
>   - ผล: 2,000 อยู่ในฐาน VAT+ยอดรวม **แต่ไม่โผล่ที่ไหนเลย** (ไม่อยู่ซับโทเทิล·ไม่มีบรรทัด) → ใบอ่านแล้วไม่ reconcile (20,000 → VAT 1,540 → 23,540 มี 2,000 หายไป)
>   - **= regression จาก fix จุด4** (เดิม Protection เป็น `lines.push` 1 บรรทัด โชว์ระหว่างซับโทเทิล-VAT ยอด reconcile ถูก · จุด4 ย้ายไป _hidden+note → บรรทัดหาย เงินยังอยู่)
>   - ⚠ **กระทบทุกใบทั้งระบบ** (svc-protect default ติ๊กทุกใบ ไม่เฉพาะ G6)
>   - **มติพี่นัท 18มิ.ย. (เคาะแล้ว):** **รวม Protection เข้าราคาชุดหลักของแต่ละรายการ (default · ใบสะอาด ยอด reconcile) + ปุ่ม "แยกราคา" ค่อยดึงออกมาเป็นบรรทัด** · ยอดรวมทั้งสิ้นเท่าเดิมทั้ง 2 โหมด
>   - **📄 CODE spec พร้อม implement:** `docs/_cross-group→งานข้ามกลุ่ม-G1ถึงG6/CODE-E1-Protection-รวมในราคา+ปุ่มแยก-2026-06-18.md` (anchors + โค้ดเสนอ + จุดเคาะหลายรายการ + guardrails golden/accountant/ทุกทางออกใบ)
> - **E2 (เช็คซ้ำ):** สีมีรหัส (อบพิเศษ/ลายไม้อบพิเศษ/ชุบ) ในห้อง G6 — ไม่มีช่องกรอกรหัส + ใบไม่โชว์รหัสสี (ต่างจาก G1 ที่มี hasCode) · = checklist ข้อ 4
> - หลักฐาน: `docs/กลุ่ม6-กั้นห้องกระจก/เทสใบ-G6-5งานต่างกัน-2026-06-18.html` (เทียบ 5 ใบ + ตารางวิเคราะห์ VAT)

> ฐาน G6 ตอนนี้ ✅: ใบ 5 บล็อก (จุด 1-4) เสร็จ · golden 149 · เทส 77/77 · commit e3adf96+50657df · Protection→หมายเหตุไทย auto
> ที่เหลือด้านล่าง = งานที่ "ไม่ใช่ G6 เดี่ยว" หรือเป็น UX/สเปก ที่ยังไม่ได้ส่ง dev

> 🟢🟢 **อัปเดต 18มิ.ย. (เทสเครื่องคิดราคาจริงในเบราว์เซอร์ + grep โค้ดปัจจุบัน):** **B แก้เกือบครบทั้งหมดแล้ววันนี้** (ไฟล์เปลี่ยนจากที่อ่านตอนต้นเซสชัน · บทเรียน: ก่อนรายงานต้องเทสสด/re-read) — verify:
> - **A1 ✅** ชิปยกเลิกขาย opacity .55 จาง·ไม่มีคำ"ยกเลิกขาย"·rule generic (L275/L3769/L5487/L5513) — เทสสด E-series
> - **A2 ✅** กระจก dropdown 66ตัว/5หมวด ราคาขยับ ฿20,000→฿21,440(เทมเปอร์) · สีอลูชิป+จุดสี COLORS เต็ม colorPrice ฿21,440→฿25,440(ลายไม้) — เทสสด (L6649-6660)
> - **B1 ✅** กล่อง🔧ใช้บ่อย(มือจับ·ล็อก·มุ้ง·คาด·ลูกฟูก)+พับ⚙เพิ่มเติม(สีเฟรม·ทาสี·ครอบวงกบ) (L6520) — เทสสด
> - **B2 ✅** ชนิดการเปิด=ชิป (select ซ่อน sync · L4288) — เทสสด 0 โชว์/1 hidden
> - **C1 ✅** disclaimer ครบ 4: เทพื้น/ลงเข็ม(L1953)·ย้ายแอร์(L1955)·สมาร์ทบอร์ด(L1957)·ระบบน้ำ(L1958)
> - **C2 ✅** g6rRoomSpec L6909 ขึ้นสี/กระจกจริงต่อช่อง(distinct ไม่ hardcode)+สีโครงหลังคา
> - **เหลือแค่ D1/D2** = งานเอกสาร (แก้ทะเบียน L89 + เพิ่ม G6 เข้า dev-status.mjs) ไม่ใช่ feature
> **= งาน dev G6 ทั้งหมดในก้อนนี้ ปิดแล้ว เหลือเอกสาร** · ส่วน A1 "ทุกกลุ่ม" คิวกลางเช็คกลุ่มอื่นว่าใช้ rule generic นี้ครบไหม

---

## 🔴 REGION 1 — กระทบทุกใบ / ระบบทั้งเว็บ (ทำก่อน)

### A1 · ปุ่ม/ชิปรุ่นที่ "ยกเลิกขาย" — ลบคำ + ทำสีจาง + ยังขายได้ (ทุกกลุ่ม)
**มติพี่นัท 17 มิ.ย.:** คงปุ่มไว้ · สีจาง (dim) · ลบคำ "(ยกเลิกขาย)" ออกหมด (ทั้งชิปและใบ) · กดได้ ขายได้ขึ้นใบปกติ · **ทำทุกบานที่ยกเลิกขาย ทุกกลุ่ม**
- สโคป (grep): PRODUCTS 4 ตัวมี `(ยกเลิกขาย)` — `บานเลื่อน E-series aluinch` L655 · `กระทุ้ง aluinch` L658 · `บานเปิด D-series` L660 · `บานหมุน aluinch` L667
- แก้: ① ชิป = เอาคำออกจาก label + class สีจาง (opacity:.55) แต่ click ทำงาน · ② ในใบ = **ลบ L5356-5358** (เลิกเติม suffix) · ③ ทำเป็น rule เดียว (ตรวจ `name.includes('ยกเลิกขาย')`) ครอบทุกบาน
- ⚠ golden: ข้อความ ไม่กระทบราคา · verify golden 149 หลังแก้

### A2 · ห้อง G6 เพิ่ม "เลือกกระจก + สีอลูครบ ต่อช่อง" (ราคาขยับจริง) — กระทบทุกใบ G6
**มติพี่นัท 17 มิ.ย. (สโคปเต็ม):** ห้องยัง**ไม่มีช่องเลือกชนิดกระจกเลย** (g6rRoomSpec L6735 พิมพ์ "เขียว/ใส 6มม." ตายตัว) · สีเฟรมมีแค่ ขาว/ดำ/เทา (L6656)
1. **กระจก = เลือกเต็ม + ราคาขยับ (เหมือน G1)** — เพิ่มช่องเลือกกระจก **ต่อช่อง** ใน renderEdit (drill ชนิด→หนา · reuse `GLASS` L603 + `GLASS_CATS` L1046 · ราคาตาม s/ตร.ม.) · เก็บ glass index ต่อ column
2. **สีอลู = เพิ่มครบ + คิดค่าสีพิเศษ** — ขยาย "สีเฟรม" → `COLORS` เต็ม (L966) · reuse `colorPrice` L1148 · ขึ้นชื่อสีจริงในใบ
- หลักฐานใบจริงโอ๋ QT2025010036: "อลู สีดำ · กระจกฝ้า (บานเลื่อน) · ลามิเนตใส ฟิล์มเขียว 5+5 มม." (2 กระจกต่างช่อง) → ระบบออกไม่ตรงเลย
- ⚠ **แตะราคา** → golden ต้องเพิ่ม baseline ห้อง · reuse engine เดิม (GLASS.s / colorPrice) ห้ามคิดสูตรใหม่
- **ดราฟกดเล่น (เทสแล้ว):** `docs/กลุ่ม6-กั้นห้องกระจก/DRAFT-G6-UX-เลือกกระจกสี-v2-2026-06-17.html`

---

## 🟡 REGION 2 — UX ฟอร์มห้อง G6

### B1 · รวบ "ออปชั่นใช้บ่อย" เป็นกล่องเดียว (ข้อ 9 · มีดราฟใหม่)
- **ปัญหา (g6rRenderOpt L6635-6680):** เรียงแบน — มือจับ → **สีเฟรม → ทาสี** (คั่นกลาง) → มุ้ง → คาดตาราง (แยกหัวข้อ) · ออปชั่นใช้บ่อยถูกแยกออกจากกัน
- **แก้:** รวบ **มือจับ·ล็อก·มุ้ง·คาดตาราง·แผ่นล่าง(ลูกฟูก/คอมโพ)** เป็นกล่อง "🔧 ใช้บ่อย" เดียว (เหมือน G1 L2858) · ย้าย สีเฟรม/ทาสี/ครอบวงกบ/ดรอปพื้น/อุปกรณ์เสริม ลงกล่องพับ "ใช้น้อย" · **แก้แค่ลำดับ section ไม่แตะ logic/ราคา**
- **ย้ำ (พี่นัท 18มิ.ย.):** "แผ่นล่าง ลูกฟูก 3,500/คอมโพสิต 3,300" = **ใช้บ่อย** ขึ้นกล่องใช้บ่อย ไม่พับ (เดิมอยู่ใต้หัวข้อ "เสริมกระจก" คู่คาดตาราง L6667)
- **ดราฟ ก่อน→หลัง:** `docs/กลุ่ม6-กั้นห้องกระจก/DRAFT-G6-UX-จัดกลุ่มออปชั่นห้อง-2026-06-18.html`

### B2 · "ชนิดการเปิด" ยังเป็น dropdown → chipify
- ช่อง "ชนิดการเปิด" (`— เลือกลักษณะการเปิด —`) ยังเป็น `<select>` ทั้งที่ตัวอื่นเป็นชิปหมด → chipify (`chipifySelect` generic · ดู [[chipify-option-dropdowns]]) · เป็น selector ใช้ร่วมหลายกลุ่ม — คิวกลางตรวจ scope ก่อนแก้

---

## 🟡 REGION 3 — ข้อความ/สเปกในใบ

### C1 · เพิ่ม disclaimer งานห้อง เข้า REMARK_PRESETS (audit B)
- มี preset 'กั้นห้องกระจก' (L1974 · 11 ข้อ) แต่เป็นแนว "คำถามเช็ค" ไม่ใช่ข้อความลงใบ
- ขาดข้อความที่ใบจริงโอ๋มี (grep=0): "ไม่รวมงานย้ายแอร์เดิม" · "ไม่รวมงานผนังสมาร์ทบอร์ด (ลูกค้าทำเอง)" · "ไม่รวมระบบน้ำ" · "ไม่รวมเทพื้น/ลงเข็ม" → เพิ่มเป็น preset ติ๊กได้ · **คิวกลางเคาะคำเป๊ะจากทะเบียนเฟิร์น** [[quote-text-registry]]

### C2 · สี/รหัสหลังคา ลงบล็อก "รายละเอียด" (audit C · minor)
- หลังคาขึ้นใน "รายการงาน" แล้ว แต่ **สี/รหัสหลังคา** ไม่ไปบล็อกสเปค → `g6rRoomSpec` push ชนิด+สีหลังคา ถ้ามีหลังคา

---

## 🟢 REGION 4 — เอกสาร/เครื่องมือ (เล็ก)
- **D1:** `docs/HANDOFF-ใบเสนอราคา-ข้อความ+สเปก-2026-06-17.md` L89 ยังเขียน "G6 พังอยู่ รอ B แก้ด่วน" → ของจริงเสร็จแล้ว แก้เป็น "G6 ✅ 5บล็อก เสร็จ 17มิ.ย."
- **D2:** `scripts/dev-status.mjs` ยังไม่ track G6 → เพิ่ม entry G6 (ใบ5บล็อก ✅ · Protection ✅ · พื้น/พัดลม ✅) กันลืม [[multichat-dev-workflow]]

---

## ✅ ปิดแล้ว / ไม่ทำ (ไม่ต้องส่ง dev)
- **ระแนงเกล็ด Z** ในใบ G6 → ออกเป็น **item G2 แยกบล็อกในใบเดียวกัน** (เซลล์กดเพิ่ม · ระบบรองรับแล้ว addGlasshouseSet+addItem ลง #items ตัวเดียวกัน) · **ราวกันตกเหล็ก = ไม่ทำ** (ใช้ราวกระจก imp1-6 เดิม) — มติพี่นัท 17 มิ.ย.
- **~~OPTION accordion พับในห้อง (ข้อ 10 เดิม)~~** = **เช็คผิด ตัดทิ้ง** · ห้องไม่มี accordion/OPTION-cascade อยู่แล้ว (g6rRenderOpt แบนหมด) · ถ้าอยากเพิ่ม OPTION-ทางเลือกในห้อง = งานใหม่ ต้องเคาะ

## 🔁 ฝาก G1 owner (จากการตรวจสลับ)
- **ผนังเบาใน G1 (กดผ่านปุ่ม) ไม่ลากฝ้าหลังคา** (check-g1 🟡) · ข้อ 7/11 (ชื่อปุ่มยาว/ลำดับ) ขอ G1 owner ดูเบราว์เซอร์ (grep ฟันธงไม่ได้)

---

## ลำดับแนะนำให้คิวกลาง
1. **A1** ปุ่มยกเลิกขาย (ทุกกลุ่ม · เร็ว · ข้อความ) → 2. **A2** กระจก/สีเต็มในห้อง (กระทบทุกใบ · แตะราคา · ใหญ่สุด · มีดราฟ) → 3. **B1** รวบออปชั่น (UX · มีดราฟ) + **B2** ชนิดการเปิด → 4. **C1** disclaimer + **C2** หลังคาสเปค → 5. **D1/D2** เอกสาร

## แหล่งละเอียด (ถ้าต้องเจาะ)
- ใบ 1 (4 จุด): `เข้าคิวกลาง-จากG6-2026-06-17.md`
- ใบ 2 (audit): `เข้าคิวกลาง-จากG6-audit-2026-06-17.md`
- ใบ 3 (swap UX): `เข้าคิวกลาง-จากG6-ตรวจสลับUX-2026-06-17.md`
- เทียบก่อน-หลัง 5 บล็อก: `docs/กลุ่ม6-กั้นห้องกระจก/เทียบก่อนหลัง-G6-5บล็อก-2026-06-17.html`+pdf
