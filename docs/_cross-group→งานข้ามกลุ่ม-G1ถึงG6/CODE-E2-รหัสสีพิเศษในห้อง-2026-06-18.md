# [CODE ส่งคิวกลาง · จากแชท A-G6] แก้ E2 — สีพิเศษ "มีรหัส" ในห้อง G6
**18 มิ.ย. 2026** · ⛔ A-G6 READ-ONLY ไม่แก้ index.html เอง — คิวกลาง/B implement
**ไม่ใช่งานการเงิน** (label-only · ไม่กระทบราคา/golden) · reuse pattern G1 ที่มีอยู่แล้ว

## ปัญหา (ยืนยันโค้ด + เห็นจริงในเทสใบ 4)
ห้อง G6 (A2 ที่ B เพิ่งเพิ่มเลือกสีครบ) — เลือกสีที่ `hasCode` ได้ แต่**ไม่มีช่องกรอกรหัส** + ใบโชว์แค่ชื่อสี ไม่มีรหัส ไม่เตือน
- COLORS hasCode 3 ตัว (L992-996): **สีอบพิเศษ · สีลายไม้อบพิเศษ · สีชุบ** (สีสั่งทำ โรงงานต้องรู้รหัสเป๊ะ เช่น KL10232A/RAL7022/Fuji)
- เทสใบ 4: เลือกสีอบพิเศษ → ใบขึ้น "อลูมิเนียม สีอบพิเศษ" ลอยๆ → **สั่งผลิตไม่รู้รหัส**

## G1 ทำถูกอยู่แล้ว (ต้นแบบ reuse)
- L4025: `<input class="i-colorcode" ... รหัสสี/ชื่อสี (อบพิเศษ)` placeholder "เช่น KL10232A · RAL7022 · Fuji"
- L4104-4108: โชว์ช่องรหัสเมื่อสีที่เลือก `hasCode`
- L6203 (spec): `if(_aci>0 && _ac.hasCode){ ... _asx = code ? ' · รหัส '+code : ' · ⚠ ต้องระบุรหัสสี'; }` → ลงใบ + เตือนถ้าลืม

## CODE ที่เสนอ (anchors · B ปรับเข้า pattern เดิม · ยึดชื่อฟังก์ชัน เลขบรรทัดอาจขยับ)

### 1) render ช่องรหัสในห้อง — ต่อจากชิปสี (g6rRenderOpt ~L6836)
เดิม: `if(pf.glass||pf.framecolor) more+='<div class="fl">สีอลู (เฟรม)</div>'+g6ColorChips(o.colorIdx);`
เพิ่มช่องรหัสเมื่อสีที่เลือก hasCode:
```js
if(pf.glass||pf.framecolor){ more+='<div class="fl">สีอลู (เฟรม)</div>'+g6ColorChips(o.colorIdx);
  var _selC=COLORS[parseInt(o.colorIdx)||0];
  if(_selC && _selC.hasCode){
    more+='<div class="g6r-rev"><div class="row"><span class="rl">รหัสสี</span>'+
      '<input type="text" value="'+((o.colorCode||'').replace(/"/g,'&quot;'))+'" '+
      'placeholder="เช่น KL10232A · RAL7022 · Fuji" onchange="G6RsetOpt2(\'colorCode\',this.value)"></div>'+
      (_selC.sampleConfirm?'<div class="g6r-wtot" style="color:#92400e;">⚠ สีชุบ: ลูกค้าต้องเห็นตัวอย่างจริง + ถ่ายรูปยืนยัน</div>':'')+
    '</div>';
  }
}
```
⚠ **ใช้ `onchange` ไม่ใช่ `oninput`** — เพราะ `G6RsetOpt2` เรียก `renderEdit()` (re-render เต็ม) ทุกครั้ง → ถ้า oninput จะ re-render ทุกตัวอักษร focus หลุด · onchange ยิงตอน blur ปลอดภัย · (หรือ B ทำ setter เก็บค่าโดยไม่ re-render)

### 2) g6rRoomSpec — ต่อรหัสเข้าสเปคสี (~L7001)
เดิม:
```js
var _ci=parseInt(pc.opt&&pc.opt.colorIdx)||0; var _cc=COLORS[_ci];
_cols[((_cc&&(_cc.quoteName||_cc.n))||'อบขาว').replace(/[★⚠🔖🔒]/g,'').replace(/^\s*สี/,'').trim()]=1;
```
แก้เป็น (ต่อ "· รหัส X" หรือเตือนถ้าลืม · เหมือน G1 L6203):
```js
var _ci=parseInt(pc.opt&&pc.opt.colorIdx)||0; var _cc=COLORS[_ci];
var _cnm=((_cc&&(_cc.quoteName||_cc.n))||'อบขาว').replace(/[★⚠🔖🔒]/g,'').replace(/^\s*สี/,'').trim();
if(_cc&&_cc.hasCode){ var _code=((pc.opt&&pc.opt.colorCode)||'').trim(); _cnm+=_code?(' · รหัส '+_code):' · ⚠ ต้องระบุรหัสสี'; }
_cols[_cnm]=1;
```
(เก็บ key เป็น "ชื่อสี · รหัส X" → distinct ตามรหัส · ถ้าหลายช่องใช้สีพิเศษคนละรหัส จะแยกบรรทัดถูก)

### 3) (รอง) สีโครงหลังคา A2 จุด3 — ถ้า hasCode ก็ควรมีรหัส
`g6RoofColorChips` (L6749) + g6rRoomSpec บรรทัดหลังคา (`สีโครง ...`) — ตอนนี้ roof color set ผ่าน `G6RroofColor` ค่าเดียว ไม่มีช่องรหัส · ถ้าเลือกสีโครงหลังคาเป็นสีพิเศษ จะขาดรหัสเหมือนกัน → เพิ่มช่องรหัสหลังคา (เก็บ G6R.roof.colorCode) + ต่อในสเปคหลังคา · **เคสนี้พบน้อยกว่า → ทำพร้อมกันหรือ flag ไว้ก็ได้**

## Guardrails
- **label-only ไม่กระทบราคา** — รหัสสีเป็นข้อความลงใบ ไม่เข้าสูตรคิดเงิน → golden 149 ไม่กระทบ (รัน verify ตามมาตรฐาน)
- เทส: ห้อง→เลือกสีอบพิเศษ → โผล่ช่องรหัส → กรอก KL10232A → ใบขึ้น "สีอบพิเศษ · รหัส KL10232A" · ไม่กรอก → "⚠ ต้องระบุรหัสสี" · เลือกสีปกติ (อบขาว) → ไม่มีช่องรหัส
- เทสสีชุบ → โผล่ทั้งช่องรหัส + โน้ต "ต้องเห็นตัวอย่างจริง"
- ตรงกับ G1: ใบงานบาน vs ใบห้อง สีพิเศษเดียวกันต้องโชว์รหัสเหมือนกัน
