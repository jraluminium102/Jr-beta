# 📋 แผนปรับ G6 (กั้นห้องกระจก) ตามฟอร์ม G1 ใหม่ — รอ G1 ลงจริงก่อน

> มติพี่นัท 17 มิ.ย.: ถ้าฟอร์ม G1 ผ่าน → ปรับ G6 ตามแบบเดียวกัน (G6 ใช้บานเลื่อน/เปิด/เฟี้ยม ร่วม FAM_CATS เดียวกับ G1)
> **สถานะ: รอ** — ทำหลัง G1 ลงเว็บจริง + verify เสร็จ (กันแก้ 2 ที่ชนกัน · [[one-group-at-a-time]])

## G6 ต่างจาก G1 ตรงไหน (ต้องเช็คก่อนทำ)
- G6 = **paged room builder** (g6rMount L3792) ไม่ใช่ฟอร์มบานปกติ · บานอยู่ใน "ด้านห้อง" (หลายด้าน)
- ปุ่ม "+ เพิ่มชิ้นในด้านนี้" (sub-addbtn ghMode L3005-3007) = ผสมบานในด้านกั้นห้อง — **คล้าย "ผสมบาน" G1**

## สิ่งที่ยกจาก G1 มาใช้ (ตามที่จะลง G1)
1. **มือจับ → ล็อค** (ถ้า G6 มีเลือกมือจับเยอะ) → เหลือล็อค
2. **คาดตาราง** ขึ้นเป็นของหลัก (ถ้าบาน G6 มี)
3. **ผสมบาน/ชิ้นในด้าน** — ใช้ pattern เดียวกับ G1: เพิ่มทุกชนิดง่าย + "ปรับ"(แบ่งบาน/มุ้ง) + "จัดเต็ม"(คาดตาราง/ราคาแยก/แก้ข้อความ)
4. **บานผสมคิดราคาตามชนิด** — ถ้า G6 ใช้ calcSubItemPrice เดียวกัน → ได้อัปเกรดฟรีพร้อม G1 (เช็คว่าใช้ตัวเดียวกันไหม)
5. **ใบ 5 บล็อก** — G6 ทำไปแล้วบางส่วน (e3adf96 กู้ใบ 5 บล็อก) · เทียบ sample ว่าตรงไหม

## ต้องเช็ค/ถามก่อนทำจริง
- G6 ใช้ `calcSubItemPrice` ตัวเดียวกับ G1 ไหม (ถ้าใช่ = อัปเกรดพร้อมกัน · ถ้าแยก = ต้องแก้ 2 ที่)
- "ด้านห้อง" G6 มีมือจับ/คาดตาราง/มุ้ง ต่อบานไหม (ดูของจริง)
- ของไม่บ่อยใน G6 (ฝ้า/หลังคา/งานไฟ) จัดพับแล้วหรือยัง

## ส่งมอบ
- หลัง G1 ลงจริง → Chat A ทำดราฟ G6 (เลียน G1 v7) → เคาะ → ส่งคิวกลาง
- ⛔ ตอนนี้ยังไม่ทำ · บันทึกไว้กันลืม
