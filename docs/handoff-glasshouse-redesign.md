# HANDOFF: รื้อ "กั้นห้องกระจก" ใหม่ — unify เข้าระบบชุด+บานปกติ (เลิก gh-box พิเศษ)

> สร้าง 2026-06-07 · อัปเดตหลังพี่นัทเคาะ 5 ข้อแล้ว · **ยังไม่ได้แก้โค้ด** — แชทนี้วิเคราะห์+ออกแบบ ให้แชทถัดไปลงมือ
> ไฟล์เป้าหมายหลัก: `public/calculator/index.html` (vanilla HTML/JS ไฟล์เดียว)

---

## ⭐ สถานะ 2026-06-07 — พร้อม PORT (อ่านก่อน) ✅
- **งานกั้นห้องทำเสร็จครบ + พี่นัทรีวิวผ่านแล้ว** บนไฟล์ทดลอง `public/calculator/glasshouse-preview.html`
  ลองเล่นตัวจริงล่าสุด: `https://jraluminium102.github.io/sharepicjr/glasshouse-preview.html?v=17`
- **งานของแชทนี้ = ให้อีกแชท (เจ้าของ index.html) PORT งานกั้นห้องจาก preview เข้า `index.html` แล้ว deploy Vercel พร้อมออปชั่นบาน/หลังคาที่เสร็จแล้ว**
- ✅ `index.html` ตอนนี้ **clean** (งานบาน/หลังคา commit ถึง `b2e2e0e` แล้ว ไม่มี uncommitted) → port ได้เลย
- ⚠️ **ห้าม copy preview ทับ index.html** — preview เป็น base เก่า ไม่มีงานบาน/หลังคา/มุ้ง/cover ใหม่ · ต้อง **re-apply เฉพาะ logic กั้นห้อง** ลง index.html ล่าสุด (เทียบ 2 ไฟล์)
- ข้อดี: หลังคา option chain เต็ม (รางน้ำ/ท่อ PVC/แป/ปลาย/รุ่นย่อย) ของ index.html จะถูกกั้นห้องใช้อัตโนมัติ — กรอกหลังคาได้ละเอียดกว่า preview

### Checklist PORT (เอา logic เหล่านี้จาก glasshouse-preview.html → index.html)
**ลบของเดิม (gh-box):**
1. ลบ product `{id:'glasshouse'...}`, ลบ array `GH_OPTIONS`, ลบ `'กั้นห้องกระจก'` ใน `CATS`
2. ลบ gh-box branch `if(p.glasshouse){...}` ใน `buildItemOpts` · glasshouse branch ใน calc engine · readOpt (`ghside_/ghprice/ghroof/ghroom/ghcolor/ghglass`) · `itemDetail`/genQuote/Excel branch

**เพิ่ม/แก้ของใหม่:**
3. `prodOptionsG6` → `_GM['6']` = `['บานเลื่อน','บานเปิด','ติดตาย','บานเฟี้ยม','บานกระทุ้ง','ระแนง','ระแนง-บังตา','ระแนง-ผนัง','ระแนง-เกล็ด','หลังคา','ฝ้า-ผนัง']` (บาน+ระแนง+หลังคา)
4. ปุ่ม **"+ กั้นห้องกระจก"** (ข้าง "+ เพิ่มชุด") → `addGlasshouseSet()` = addSet + ตั้งส่วนแรก group6 + position "ด้าน A" → refreshSet จัดการที่เหลือ
5. **`refreshSet(box)` เขียนใหม่เป็น GH-aware** (หัวใจ — robust ทุกทางสร้างชุดที่มี .ch group6):
   - โชว์ `.i-position` เมื่อ group6
   - ปุ่ม `.set-addpart` → text **"+ เพิ่มบาน"** + **เก็บ idx0 ซ่อนที่เหลือ** (กันปุ่มซ้ำบน/ล่าง) · กดได้รัวๆ = เพิ่มบาน auto ด้านถัดไป
   - ซ่อน `.set-addfixed` (ช่องแสงติดตายระดับชุด)
   - **inject ปุ่ม `.set-addroof` "+ เพิ่มหลังคา"** ใน `.set-btns-top` (idempotent) → กดแล้ว addItem + group3 + position "หลังคา"
   - **inject master color row** `.gh-set-color-row` "สีอลู/กระจก (ทั้งห้อง)" หัวชุด → sync ลงทุก ch group6 + **ซ่อน `.i-color-wrap`/`.i-glass-wrap`** ใน ch group6 (mark `data-gh-hidden`)
   - **ซ่อน `.sub-addbtn`** ใน ch group6 (ตัด "เพิ่มบานในด้าน" — เพิ่มบานในด้านเดิมทำผ่านเพิ่มบาน+แก้ชื่อด้านซ้ำ)
   - **ลดความรก (ทุก ch ในชุด): ซ่อน field พื้นฐาน `.i-group/.i-type/.i-qty/.i-override/.i-note/.i-ship` (mark `data-gh-h2`, ซ่อนเสมอ ไม่เช็ค guard เพราะ refreshItype คืน display) + ปิด `<details.optbox>` (`.open=false`) เปลี่ยน summary='⚙ ตัวเลือกเพิ่มเติม (มือจับ/ล็อค/มุ้ง/ครอบวงกบ)' → ฟอร์มเหลือ 5 ช่อง (ด้าน/แบบ/กว้าง/สูง/จำนวนบาน) · option พับ กดขยายได้**
   - else (ไม่มี group6): คืนทุกอย่าง (ลบ color row, คืน wrap/ปุ่ม display, คืน `data-gh-h2` + summary เดิม)
> ⚠️ **i-prod change listener ต้องเรียก `refreshSet(setbox)` ด้วย** — กัน logic ~`ob.open=!!(...)` (ราว line 2069) ที่เปิด `<details>` กลับทุกครั้งที่เปลี่ยน product
6. **`addSet` listener `.set-addpart`** → เช็ค hasGH ตอนกด: ถ้า GH → addItem + group6 + auto "ด้าน B/C/…" + sync สีจาก master row
7. **`addItem` listener `.i-group` change** → เลือก group6: auto position "ด้าน X" (ตามลำดับ ch) + label `🔴 ด้านของห้องนี้` + refreshSet · เปลี่ยนออกจาก 6: refreshSet cleanup
8. **genQuote grouped render (else branch)** → group items by `position`: หัวด้าน "ด้าน X" (สีแดง) + บานเรียงใต้ "• {ชื่อบาน}" · ชิ้นไม่มี position แนบกลุ่มด้านล่าสุด (lastKey) · ตัด join " พร้อม " ทิ้ง · ตัดชื่อด้านซ้ำเมื่อ prodName ขึ้นต้นด้วย key · `_gl` (หัวข้อข้อ) fallback = `'กั้นห้องกระจก'` เมื่อมี position
9. ลบ/เขียนเทสใหม่: `test/calc-glasshouse.mjs`, `test/gen-formats.mjs` (อิง gh-box เก่า — จะพังหลังลบ)

**⚠️ จุด overlap กับงานบาน/หลังคา — ตรวจก่อน apply:**
- `prodOptionsG6`/`_GM` · `refreshSet` · `genQuote grouped render` · `buildItemOpts` · `addItem`/`addSet` listener — อีกแชทอาจแก้จุดเดียวกัน ต้อง merge ระวัง ไม่ทับงานบาน/หลังคา
- หลังคา: **อย่าเอา product หลังคา/option chain จาก preview** (เก่ากว่า) — ใช้ของ index.html ที่มีอยู่

### อัปเดตล่าสุด (รวมตอน port ด้วย)
- **auto-label เพิ่มบาน = "ด้าน A" เสมอ** (ไม่ increment B/C) — พี่นัทขอเพิ่มบานในด้าน A ก่อน แก้เป็น B/C ทีหลัง · แก้ 2 จุด: addSet `.set-addpart` listener + addItem i-group listener (`pos.value='ด้าน A'`)
- **`groupGHOpts(ch)`**: จัด option ใน `.i-opts` เป็น **`<details>` dropdown พับได้ต่อหมวด** (summary = ชื่อหมวด + จำนวน) — หมวด: 🚪ชนิด/การเปิด · 🔧มือจับ-ล็อค · 🪟มุ้ง · 📐คาดตาราง · ➕อื่นๆ · keyword match จาก text · class `.gh-opt-cat-det` · เรียกใน refreshSet [4] (isGH) · idempotent guard `.gh-opt-cat` · ⚠️ **keyword-based เปราะ** — ตอน port ถ้าทำได้ควรให้ buildItemOpts จริงสร้าง option เป็นหมวดตั้งแต่ต้น (robust กว่า post-process)

### เหลือทำตอน port (พี่นัทเคาะ: ภาพรวม UX โอเคแล้ว 2026-06-07)
- ปรับ **ดีเทลขนาดบาน** + **option เพิ่มเติม** ตอนรวมกับงานบาน (จะได้ option chain จริงของ index.html มาเต็ม) — ไม่ต้องทำใน preview
- groupGHOpts (จัดหมวด option keyword-based) → พิจารณาทำให้ robust ตอนนี้ (สร้างหมวดจาก buildItemOpts จริง)

### ทดสอบหลัง port (พี่นัทรีวิว draft นี้ผ่านแล้ว — ใบต้องออกแบบนี้)
ชุดกั้นห้อง: ด้าน A (บานเลื่อน + ระแนงบังตา) · ด้าน B (ติดตาย) · หลังคา (ไวนิล + ชินโคไลท์) →
ใบออก **1 ข้อ** "กั้นห้องกระจก (...)" group ตามด้าน (หัวด้าน + บานใต้ •) + รายละเอียดงานรวม + ราคารวมทุกชิ้น

---

## 0. TL;DR
- **ปัญหาเดิม:** กั้นห้องกระจกใช้ gh-box พิเศษ — แต่ละด้าน A–H เลือกชนิดบานได้ชนิดเดียว, กรอกราคาเหมาเอง, หลังคาพิมพ์เอง → กรอกงานจริงไม่ได้ + ซับซ้อน
- **มติพี่นัท 2026-06-07:** *"งานกั้นห้องก็เหมือนงานประตูที่มีบานเสริมต่อกันเยอะกว่าทั่วไปในแต่ละด้านเฉยๆ"* → **รื้อใหญ่ unify เข้าระบบบานปกติ**
- **ข่าวดี:** ระบบที่ต้องการ **มีอยู่แล้วเกือบครบ** (ชุดหลายส่วน → ออกใบ 1 ข้อ + รวมราคาอัตโนมัติ) งานหลัก = **ลบ gh-box + ทำให้กลุ่มงาน 6 ใช้ระบบชุดปกติ**

---

## 1. มติพี่นัท 5 ข้อ (เคาะแล้ว — ทำตามนี้ ห้ามเปลี่ยน)
| # | คำถาม | คำตอบพี่นัท |
|---|---|---|
| 1 | รื้อแค่ไหน | **รื้อใหญ่ unify เต็ม** (เลิก gh-box) — ไม่ทำเฟส incremental |
| 2 | ราคาคิดยังไง | **คิดตามด้าน เหมือนคิดบานประตู** — แต่ละบาน/ด้านคิดราคาออโต้ตามขนาด (เลิกกรอกราคาเหมา `ghprice`) |
| 3 | หลังคา | **ใช้ระบบหลังคาเดิมคิดราคาให้** — ไม่พิมพ์เอง (เลิก free text `ghroof`) |
| 4 | บานผสม | **มีบานขนาดอื่นผสมในด้านได้ + มีช่องเติมเหมือนโหมดบาน** (ชนิด/กว้าง/สูง/จำนวน) |
| 5 | ใบเก่า | **เป็นแค่เทส ไม่ต้องซีเรียส** — ไม่ต้องทำ backward-compat/migration แก้ได้เรื่อยๆ |

---

## 2. ระบบที่ "มีอยู่แล้ว" และต้อง reuse (สำคัญ — อย่าสร้างใหม่)
ตรวจโค้ดแล้ว ระบบชุดทำสิ่งที่พี่นัทต้องการได้เกือบหมด:
| ความสามารถ | มีแล้วที่ | หมายเหตุ |
|---|---|---|
| **ชุดหลายส่วน → ออกใบเป็น 1 ข้อ + รวมราคาออโต้** | `index.html:2513-2555` ("Render grouped") | gTotal = ผลรวมทุกส่วน · ออก 1 แถว "1 ชุด" |
| ปุ่ม "+ เพิ่มส่วน" ในชุด | `set-addpart` → `addItem` `1532` | = เพิ่ม "ด้าน" |
| ช่อง "ด้าน/ตำแหน่งของส่วนนี้" (โผล่เมื่อ >1 ส่วน) | `.i-position` `1620` · `refreshSet` `1524` | ใส่ "ด้าน A/B/C" |
| **ราคาบานคิดออโต้ตามชนิด+ขนาด** | engine ปกติ (cost × area …) | = ข้อ 2 ที่พี่นัทต้องการ |
| ปุ่ม "แยกราคา/รวมราคา" ต่อชุด + โชว์ขนาดแต่ละบาน | `toggleGroupSplit` `2532-2536` | |
| ปุ่ม "+ เพิ่มบานย่อยในชุดนี้" (ช่องแสงติดตาย/บานข้าง/ช่องแสงบน) | `addSubItem` `1664` · `SUBITEM_TYPES` `1657` | = บานเสริมต่อด้าน |
| OPTION ระดับชุด คิดราคาส่วนต่าง | `set-opttext/delta` · `addSetOptPreset` `1593` | |
| ชื่อหัวข้อชุด (ออกเป็นหัวข้อในใบ) | `.set-name` → `groupLabel` `2008` | ใส่ "กั้นห้องกระจก (ห้อง...)" |
| product หลังคาแยกชนิด (ไวนิล/ลามิเนต/ชินโค/เมทัลชีท) | กลุ่มงาน 3 · ดู `quote-fill-spec.md` ข้อ 2.6 | = ข้อ 3 ที่พี่นัทต้องการ |

→ **กั้นห้องกระจก = "ชุด" 1 ชุด ที่เพิ่มหลายส่วน(ด้าน/บาน/หลังคา) ตามจริง** ทุกอย่างคิดราคาออโต้ + ออกใบเป็น 1 ข้อ

---

## 3. Design ปลายทาง
```
ตั้งชื่อชุด: "กั้นห้องกระจก (ห้องอเนกประสงค์)"
  ส่วนที่ 1  ด้าน A · ประตูบานเปิดคู่   1.8×2.2   → คิดราคาออโต้
      └ บานย่อย: ช่องแสงติดตายข้าง, ช่องแสงบน
  ส่วนที่ 2  ด้าน B · ติดตายเต็มผนัง    3.0×2.2   → คิดราคาออโต้
  ส่วนที่ 3  ด้าน C · หน้าต่างบานเลื่อน 1.5×1.2   → คิดราคาออโต้
      └ บานย่อย: ช่องแสงติดตายข้าง
  ส่วนที่ 4  หลังคา · ไวนิล แปคู่       4×3       → คิดราคาออโต้ (กลุ่มงาน 3)
─────────────────────────────────────────────
ใบออก: 1 ข้อ "กั้นห้องกระจก (ห้องอเนกประสงค์)" ราคา = ผลรวมทุกส่วน
        กด "แยกราคา" เพื่อโชว์รายด้าน
```
ตรง `quote-spec.md` ข้อ 6 (1 ข้อแตกเป็นด้าน A-H + หลังคา) อยู่แล้ว

---

## 4. งานที่ต้องทำจริง (เล็กกว่าที่ประเมินตอนแรก)
1. **ลบ gh-box ทั้งก้อน** — `buildItemOpts` ส่วน `if(p.glasshouse)` `1716-1742`, `GH_SIDE_OPTS`, `GH_OPTIONS` `767-781`, readOpt `ghside_/ghprice/ghroof` `1934-1945`, calc glasshouse branch `1210-1228`, genQuote glasshouse `2290`/`2650`
2. **กลุ่มงาน 6 "กั้นห้องกระจก"** — เปลี่ยนจาก "เลือก product glasshouse" เป็น **preset ที่จัดให้ใช้ระบบชุดปกติ**:
   - กดกลุ่ม 6 → ตั้งชื่อชุด default "กั้นห้องกระจก (...)" + เปิดโหมดหลายส่วน (โชว์ช่องด้านทันที)
   - ปุ่ม "+ เพิ่มส่วน" อาจ rename เป็น "+ เพิ่มด้าน" เมื่ออยู่ในชุดกั้นห้อง + auto-เติม `.i-position` = "ด้าน A/B/C…" ตามลำดับ
   - แต่ละส่วนเลือกบานหลักจาก product ปกติ (ประตู/หน้าต่าง/ติดตาย) — มีช่องกว้าง/สูง/จำนวนอยู่แล้ว (= ข้อ 4 บานผสม ✓)
3. **หลังคา** — เพิ่มเป็นส่วนที่เลือก product หลังคากลุ่ม 3 (ไม่ใช่ field พิมพ์เอง)
4. **ชื่อหัวข้อในใบ** — ให้ `.set-name` = "กั้นห้องกระจก (ชื่อห้อง)" แสดงเป็นหัวข้อ (มี groupLabel อยู่แล้ว `2008`)
5. **กระจก/สีอลู** — ถ้าอยากกรอกครั้งเดียวทั้งห้อง: reuse logic sync จาก `addFixedToSet` `1550` (ดึงค่าจากบานแรก)

---

## 5. UX fixes พ่วง (ทำตอนรื้อเลย)
| # | ปัญหา | แก้ |
|---|---|---|
| U1 | (จะหายเองหลังลบ gh-box) กล่องหลักเคยถูกซ่อนใน `<details>` "อุปกรณ์เสริม/ออปชั่น" `1634` | ระบบชุดไม่ได้ซ่อน — หายเอง |
| U2 | โชว์ด้านเท่าที่เพิ่มจริง (เลิกโชว์ครบ 8 ด้านเสมอ) | ระบบชุด "เพิ่มส่วน" ทำได้เลย |
| U3 | หลังคาเป็น dropdown chain (ชนิด/แป/ปลาย/รางน้ำ) | ดู `quote-fill-spec.md` ข้อ 2.6 — อาจทำทีหลังถ้า product หลังคาเดิมยังไม่มี chain |
| U4 | chip สรุป "กรอกแล้ว 5 ด้าน" ที่หัวชุด | optional |

---

## 6. จุดที่ต้องตรวจก่อนลงมือ (ไม่ใช่ถามพี่นัท — ให้แชทถัดไปเช็คโค้ด)
1. **product หลังคาในกลุ่มงาน 3** มีครบไหม (ไวนิล/ลามิเนต/ชินโค/เมทัลชีท + โครงอลู) — ดู `prodOptionsG6('3')` · ถ้าขาดต้องเพิ่ม
2. **การคิดราคาบานในชุด** — ยืนยันว่า engine คิดราคาต่อบานถูกเมื่อบานอยู่ในชุด (ดู readItem `2002` + calc)
3. **auto-label ด้าน** — ใส่ "ด้าน A/B/C" ลง `.i-position` อัตโนมัติทำที่ไหน (ตอน addItem ในชุดกั้นห้อง)
4. **หัวข้อชุดเปล่า** — ถ้าผู้ใช้ไม่ตั้งชื่อชุด ใบต้อง fallback เป็น "กั้นห้องกระจก" ไม่ใช่ค่าว่าง

---

## 7. เทสที่ต้องรื้อด้วย (พี่นัท: ใบเก่าเป็นแค่เทส แก้ได้)
- `test/calc-glasshouse.mjs` (ใช้ `.o-ghside-A`/`.o-ghprice` — จะถูกลบ) → เขียนใหม่ทดสอบ flow ชุดหลายส่วน
- `test/gen-formats.mjs` — มี test case glasshouse แบบเก่า อัปเดต
- เพิ่มเทส: ชุดกั้นห้อง 3 ด้าน + บานย่อย + หลังคา → ใบออก 1 ข้อ ราคา = ผลรวม

---

## 8. ไฟล์อ้างอิง
- โค้ดหลัก `public/calculator/index.html`:
  - **เก็บ/reuse:** ชุด `addSet 1525` · `set-addpart 1532` · `refreshSet 1524` · grouped render `2513-2555` · บานย่อย `1656-1700` · OPTION ชุด `1593`
  - **ลบ:** gh-box `1716-1742` · `GH_SIDE_OPTS` · `GH_OPTIONS 767-781` · ghside/ghprice/ghroof readOpt `1934-1945` · calc/genQuote glasshouse branch `1210-1228`,`2290`,`2650`
- spec: `docs/quote-spec.md` ข้อ 6 + ข้อ 11 · `docs/quote-fill-spec.md` ข้อ 2.6/2.7
- mockup บานย่อย: `docs/mockup-เพิ่มบานย่อย.html`
- คู่มือผู้ใช้ (อัปเดตหลังแก้): `คู่มือ/คู่มือกรอกใบเสนอราคา.html`
