# 📋 ใบสั่งงาน G5 มุ้ง — รอบ 2 (เข้าคิวกลาง · 17 มิ.ย. 2026)

> แชทตรวจ G5 เตรียม · ⛔ ไม่ส่ง Chat B ตรง — เข้า `docs/คิวงาน-dev-master-2026-06-17.md` ก่อน
> รอบ 1 (engine คิดต่อบาน + กันแมว + ใบ5บล็อก) Chat B แก้แล้ว (commit b3d9c98 · verify ✅)
> **สำคัญ:** รัน `node scripts/golden-snapshot.mjs` ก่อน-หลัง · 149 ตัวห้ามเพี้ยน

---

## ✅✅ อัปเดตสถานะ 18 มิ.ย. — รอบ 2 (#4–#8) Chat B แก้เสร็จหมดแล้ว · verify ครบ · **ปิดได้ ไม่ต้องสั่ง B ซ้ำ**

> แชทตรวจ G5 ตรวจซ้ำของจริง index.html (อย่าเดา) 18 มิ.ย. → ทุกข้อในรอบ 2 ทำแล้ว:

| ข้อ | งาน | หลักฐาน verify | สถานะ |
|-----|-----|----------------|-------|
| #4 | สีกรอบมุ้งเฟรมต้องคิดเงิน | engine L1346 คิด colorPrice×0.5 · เทส imp21 3.0×2.0 = **Δ +2,000** (เคสเล็ก 1.0×2.0 Δ0 เพราะ roundUp พันกลืน ~540 = ปกติ) | ✅ เสร็จ |
| #5 | ครอบวงกบมุ้งเฟรมต้องคิดเงิน | engine L1347-1350 · เทส imp21 ครอบวงกบ 3 ด้าน = **Δ +3,000** | ✅ เสร็จ |
| #6 | "ห้าม PC" ออกจากใบ | ชื่อ imp21 L772 = "มุ้งเฟรมเล็ก ผ้ามุ้งไฟเบอร์ (บานเปิด/เลื่อน/กระทุ้ง/ยก)" — ไม่มี "ห้าม PC" แล้ว · deny_with เก็บไว้ | ✅ เสร็จ |
| #7 | typo รุ่ง→รุ่น | ชื่อ imp28 L775 = "...รุ่นรางเตี้ยแบบพิเศษ..." | ✅ เสร็จ |
| #8 | กันแมว/หมา→กันแมว (รวม G1) | L1868 / L5890 / L6002 ทุกจุด `anti_pet:'ผ้ากันแมว'` — ไม่มี "/หมา" แล้ว | ✅ เสร็จ |
| #8b | ชื่อ product imp33 (เคาะพี่นัท 18มิ.ย.) | ชื่อ imp33 **L782** ยังเป็น `"มุ้งกันแมว หมา"` (ซ่อนจากชิป · ใบเก่าที่เลือก imp33 ยังโชว์ "หมา") → **มติพี่นัท: แก้เป็น `"มุ้งกันแมว"`** (ตัด " หมา") · dev แก้ชื่อ PRODUCTS L782 · ไม่กระทบราคา (golden นิ่ง) | 🟡 รอ B |

**golden-snapshot: 149 ตัวตรง baseline ✅ (ราคาไม่เพี้ยน · #4/#5 default ไม่บวก)**
**โหมด B ติดบานเดิม Δ0** = ถูก (ต้องใส่ค่าเสริมกล่อง screen_extra ถึงบวก L1344 · เทสไม่ใส่จึง Δ0 — ไม่ใช่บั๊ก)

### 🟨 #10 ใบ imp21 ผ้าซ้ำ 2 ที่ (โซน 3 ใบ · A-G1 สลับตรวจเจอ · G5 เจ้าของ verify+ยืนยันวิธีแก้แล้ว 18มิ.ย.)
- **ปัญหา (verify ของจริง):** ใบ imp21 — รายการบน = "มุ้งเฟรมเล็ก **ผ้ามุ้งไฟเบอร์** บานเลื่อน" + รายละเอียด = "**ผ้าไฟเบอร์ดำ**" → ผ้าโผล่ 2 ที่
- **ที่มา:** ชื่อ imp21 (L772) มี "ผ้ามุ้งไฟเบอร์" ในตัว · `_mqNm` (L5730) ตัดแค่วงเล็บ ไม่ตัดคำผ้า → ผ้าติดมากับรายการบน · ขณะที่ screen_addon ก็ push ผ้าลง `_mSpec` (L5722) อีกที = ซ้ำ
- **วิธีแก้ (ยืนยัน · ตรงมติ [[quote-item-structure]] 16มิ.ย. ผ้า=สเปคอยู่รายละเอียด ไม่ใช่รายการบน):** ใน `_mWork` ตัดคำผ้าออกจาก `_mqNm` **เฉพาะเมื่อผ้าถูก push ลง `_mSpec` แล้ว** (คือ `_sfM` screenFabric ถูกเลือก = imp21/23) — เช่น `if(_sfM) _mqNm=_mqNm.replace(/\s*ผ้า(มุ้ง)?ไฟเบอร์\S*/,'').replace(/\s+/g,' ').trim();`
- **⚠️ caveat (verify แล้ว):** imp28/29 (non-screen · ผ้าอยู่ในชื่ออย่างเดียว ไม่มี screenFabric → ไม่ push _mSpec ผ้า) **ต้องไม่ตัด** ไม่งั้นผ้าหายจากใบ · เงื่อนไข `if(_sfM)` กันให้แล้ว (imp28 _sfM='' → ไม่ตัด ✅)
- เสี่ยงกลาง-สูง (แตะ render ใบ L5709-5734) → ⛔ ตรวจ/เคาะก่อนสั่ง B · เทสหลังแก้: ใบ imp21 รายการบน="มุ้งเฟรมเล็ก บานเลื่อน" · รายละเอียด="ผ้าไฟเบอร์ดำ" (ไม่ซ้ำ) · ใบ imp28 ผ้ายังอยู่

➡️ **เหลือ 3 งานรอ B (พี่นัทสั่ง "รอตรวจรวบก่อน · ยังไม่ปล่อย B" 18มิ.ย.):** (โซน 2) #8b imp33 ชื่อ "หมา"→"มุ้งกันแมว" · (โซน 3) #10 ใบ imp21 ผ้าซ้ำ ⬆️ · (โซน 4) สีกรอบมุ้ง dropdown→ชิป+จุดสี (ดราฟ blueprint พร้อม) — ⛔ ทั้งหมดเข้าคิวกลาง รอแชทกลาง/พี่ตรวจรวบแล้วค่อยปล่อย

---

## 📦 (อ้างอิง) ใบสั่งเดิมรอบ 2 — ทำแล้วทั้งหมด ⬆️

## 🟩 โซน 2 — ราคา/สเปก (engine + product data)

### #4 สีกรอบมุ้งเฟรม (imp21/22/23) — ต้องคิดเงิน + ขึ้นใบ 🔴
- **ปัญหา:** เลือกสีกรอบ (o-mscolor) ได้ แต่ราคาไม่ขยับ (Δ0) + ไม่ขึ้นใบ
- **ที่มา:** engine `screen_addon` (L1164-1193) `return` ที่ L1193 ก่อนถึงโค้ด mscolor ปกติ → ไม่ handle `optSel.mscolor`
- **เทียบ:** มุ้ง non-screen (imp28/mj_blackout/imp29) คิดสีกรอบ +1,000 ปกติ · ใบจริงระบุสีกรอบเฟรม (เทาซาฮาร่า/อบขาว/ลายไม้)
- **แก้:** ในบล็อก screen_addon ก่อน return — เพิ่มคิด colorPrice (เหมือน engine มุ้งปกติ) + push "สีกรอบมุ้ง ..." ลง _mSpec (L5487 มีโค้ด push สีกรอบอยู่แล้ว · แค่ทำให้ screen_addon ผ่านเข้าได้)

### #5 ครอบวงกบมุ้งเฟรม (imp21/22/23) — ต้องคิดเงิน 🔴
- **ปัญหา:** ปุ่มครอบวงกบ (o-fcsides 3/4 ด้าน) เลือกได้ แต่ราคาไม่ขยับ (Δ0)
- **ที่มา:** screen_addon `return` L1193 ก่อนถึงโค้ดครอบวงกบ L1578 (`opt += ffr×flen`)
- **แก้:** ย้าย/สำเนา logic ครอบวงกบ (L1578) มาคิดก่อน return L1193 สำหรับ screen_addon (ลบ warning L1192 "ไม่รวมเสริมวงกบ" ออก เพราะคิดแล้ว)

### #7 typo "รุ่ง"→"รุ่น" 🟡
- ชื่อ PRODUCTS imp28 **L768**: `"มุ้งจีบ ตีนตะขาบ รุ่งรางเตี้ยแบบพิเศษ ผ้ามุ้งไฟเบอร์"` → แก้ `รุ่ง`→`รุ่น`

### #8 "ผ้ากันแมว/หมา"→"ผ้ากันแมว" (มติแก้หมดทุกจุด) 🟡
- **L1713** `fabricLabel`: `anti_pet:'ผ้ากันแมว/หมา'` → `'ผ้ากันแมว'`
- **L5659** `_mfMapSl`: `anti_pet:'ผ้ากันแมว/หมา'` → `'ผ้ากันแมว'`
- **L5771** `fabricMapM`: `anti_pet:'ผ้ากันแมว/หมา'` → `'ผ้ากันแมว'`
- ⚠️ **3 จุดนี้เป็น G1 มุ้งติดบาน (key `anti_pet`)** — คนละจุดกับ G5 ขายเดี่ยว (key `cat` ที่ Chat B แก้แล้ว L5483). แจ้งแชท G1/คิวกลางว่าจุดนี้กระทบ G1
- (เช็คซ้ำ optional: ชื่อ product `imp33` L773 = "มุ้งกันแมว หมา" ซ่อนจากชิปแล้ว แต่ชื่อยังมี "หมา" — ใบเก่าที่มี imp33 จะโชว์ "หมา" · จะแก้ชื่อด้วยไหม? รอเคาะ)

## 🟨 โซน 3 — ใบ 5 บล็อก / ข้อความ

### #6 "ห้าม PC" เอาออกจากใบ 🔴
- **ปัญหา:** ใบเสนอราคา imp21 โชว์ "...มุ้งเฟรมเล็ก ผ้ามุ้งไฟเบอร์ **ห้าม PC** บานเลื่อน..." — "ห้าม PC" เป็นโน้ตเตือนภายใน (ห้ามจับคู่ประตู PC Door · ระบบกันด้วย deny_with แล้ว) ไม่ควรให้ลูกค้าเห็น
- **ที่มา:** ชื่อ imp21 **L765** มี "ห้าม PC" นอกวงเล็บ → ใบ 5 บล็อก `_mqNm` (L5501) ตัดแค่ในวงเล็บ → หลุด
- **แก้ (เลือก 1):** (ก) ตัด " ห้าม PC" ออกจากชื่อ imp21 L765 (เหลือ "มุ้งเฟรมเล็ก ผ้ามุ้งไฟเบอร์ (บานเปิด/เลื่อน/กระทุ้ง/ยก)" · deny_with เก็บไว้) — **แนะนำ** · หรือ (ข) ย้าย "ห้าม PC" เข้าวงเล็บ
- หลักการถาวร: โน้ตเตือนภายในห้ามขึ้นใบลูกค้า (ดู memory quote-no-internal-notes)

## ✅ เช็คหลังแก้
1. `node scripts/golden-snapshot.mjs` → 149 ตรง baseline (จุด #4/#5 แตะ engine มุ้ง · ราคามุ้งจะขยับ = ต้อง --save baseline ใหม่หลังยืนยันถูก)
2. `node scripts/test-g5-optprice.mjs` → สีกรอบเฟรม/ครอบวงกบเฟรม ต้องขึ้น 🟢 (เดิม 🔴)
3. `node scripts/check-g5.mjs` → 🔴0
4. ออกใบ imp21 → ไม่มี "ห้าม PC" · imp28 = "รุ่น" · ผ้า = "กันแมว" (ไม่มีหมา)

## ⚠️ หมายเหตุก้อนชนกัน
- #4/#5 แตะ engine มุ้ง (L1164-1193) → ราคามุ้งเฟรมจะเปลี่ยน · golden ต้อง re-baseline หลังยืนยัน
- #8 กระทบ G1 (มุ้งติดบาน) → ไม่ใช่ G5 ล้วน · แชทกลางพิจารณาจัดคิวร่วมงานตรวจ G1

---

## 🎨 ตรวจ UX แม่แบบร่วม ข้อ 5-12 (17มิ.ย. · เครื่องมือ `scripts/ux-audit-g5g4.mjs`)

### G5 มุ้ง (กลุ่มตัวเอง)
- ✅ ข้อ 5 ใบ 5 บล็อก · ✅ ข้อ 6 ผ้ามุ้ง/ครอบวงกบ/สีผ้า/ประเภท เป็นชิปแล้ว (select ซ่อน sync) · ✅ ข้อ 7 ปุ่มรุ่นใช้ label สั้น ("เฟรมเล็ก") · ✅ ข้อ 9 ผ้า+สีกรอบรวมในกล่องมุ้ง · ✅ ข้อ 10 OPTION block มี
- 🟡 **ข้อ 8: สีกรอบมุ้ง `o-mscolor` 12 สี ยังเป็นดรอปดาวน์ (L4534)** → ควรชิปหมวด+จุดสีกลม · **→ เข้าโซน 4 chipify (ไม่ใช่บั๊กการเงิน)**
  - **💡 dev ทำได้เลย: ใช้ของที่มีอยู่แล้ว** — `COLOR_CATS` (L1002) + `COLOR_SWATCH` (L1009) + `colorDrillRender()` (L1019) ที่ขับสีอลู `.i-color`/`#global-color` อยู่ · สีกรอบมุ้ง o-mscolor = สีอลู index เดียวกัน (i≥2) → ผูก drill เดียวกันได้ (filter เฉพาะ option ที่มี = index 2-12 · หมวด std ไม่โผล่)
  - **ดราฟ blueprint พร้อม:** [DRAFT-G5-ux-2026-06-17.html](../DRAFT-G5-ux-2026-06-17.html) ทำสีกรอบเป็นชิปหมวด(3)+จุดสีกลมแล้ว — เปิดดูหน้าตา/พฤติกรรมที่ต้องการได้ · verify เบราว์เซอร์จริง: เลือกสักทอง ราคา 5,000→7,000 ✅

### G4 ตู้ (ตรวจสลับ · มุมคนนอก)
- ✅ ข้อ 5 ใบ 5 บล็อก · ✅ ข้อ 6 ตู้อลูเป็นชิปหมด · ✅ ข้อ 10 OPTION block มี
- 🟡 **ข้อ 8: สีฝาตู้ Future Tech `o-ftcolor` 12 สี ยังเป็นดรอปดาวน์** → ควรชิป+จุดสี · **→ เข้าโซน 4 chipify** (แจ้งแชท G4/คิวกลาง — จุดนี้อยู่ใน G4)

> ทั้ง 2 จุดข้อ 8 = UX สี dropdown→ชิป · ⛔ ไม่แก้เอง (โซน 4 แชทกลางจัดทั้งระบบ) · บันทึกในคิวมาสเตอร์ โซน 4 แล้ว
> ข้อ 11 (ลำดับครอบวงกบล่าง/พับ) · ข้อ 12 (ข้อความตรงใบจริง) — ตรวจตา/รอบก่อนผ่านแล้ว
