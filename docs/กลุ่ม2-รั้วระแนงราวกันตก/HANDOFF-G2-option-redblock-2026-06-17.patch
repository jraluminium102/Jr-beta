diff --git a/public/calculator/index.html b/public/calculator/index.html
index 387df92..42542d9 100644
--- a/public/calculator/index.html
+++ b/public/calculator/index.html
@@ -5327,6 +5327,7 @@ function genQuote(keepNo){
        ใบจริงโชว์เฉพาะวัสดุ (กระจก/สีอลู) ใต้ "รายละเอียดงาน" ไม่โชว์ราคาย่อย */
     // extraDesc ถูกลบแล้ว — หมายเหตุ/สเปกพิเศษใช้ i-note แทน
     let det = '';
+    let gateOptNote = ''; // G2-12: OPTION ลายไม้ Golden Teak SMS — ส่งผ่าน note ให้ qrow แสดงบล็อก OPTION (แดง) ไม่ใช่ spec
     var _g1FB=false; // G1 ใบ 5 บล็อก: ตั้ง true ใน branch บานประตู/หน้าต่าง (slide+else) เท่านั้น → post-process แยก รายการ/รายละเอียด
     if(it.p.fin){
       var _finColor=(it.finLabel||'ไม่อบสี');
@@ -5441,10 +5442,10 @@ function genQuote(keepNo){
       var _gSpec=[];
       if(_grnp){ _gSpec.push('ลายระแนง: '+_grnp.name.replace(' ไม่โครง','').replace(/\s*\(ข้อ[^)]*\)/g,'').trim()); }
       var _gfinTxt=_go.gfin||'สีอบขาว'; if(_gfinTxt) _gSpec.push(/สี/.test(_gfinTxt)?_gfinTxt:'สี'+_gfinTxt); // R2-4: กันคำซ้ำ
-      // G2-12: OPTION ลายไม้ Golden Teak SMS — คง "OPTION :" prefix ให้ qrow จัดสี · ไม่ใส่ใน _work/_spec
+      // G2-12: OPTION ลายไม้ Golden Teak SMS — ส่งผ่าน gateOptNote → note (qrow จัดบล็อก OPTION แดง) ไม่ต่อท้าย spec หลัง marker
       var _gwd=parseFloat(_go.gatewood)||0;
-      var _gOptNote=(_gwd>0)?('OPTION : เปลี่ยนเป็นอลูมิเนียมลายไม้ สี Golden Teak SMS ราคาเพิ่ม '+fmt(_gwd)+' บาท'):'';
-      det=_gWork.join('\n')+'\n###รายละเอียด###\n'+_gSpec.join('\n')+(_gOptNote?'\n'+_gOptNote:'');
+      if(_gwd>0) gateOptNote='OPTION : เปลี่ยนเป็นอลูมิเนียมลายไม้ สี Golden Teak SMS ราคาเพิ่ม '+fmt(_gwd)+' บาท';
+      det=_gWork.join('\n')+'\n###รายละเอียด###\n'+_gSpec.join('\n');
     }
     else if(AREA_CATS.indexOf(it.p.cat)>=0 && it.p.cat!=='หลังคา'){ det = '-'; }  // ข้อ40: หลังคาไม่ใช้ '-' — ไปเข้า branch หลังคา (โครงอลู)
     else if(it.p.zipscreen){
@@ -5792,7 +5793,7 @@ function genQuote(keepNo){
       });
       if(!_g1Work.length && !_g1Spec.length){ _g1Work=null; _g1Spec=null; }
     }
-    return {prodName, det, work:_g1Work, spec:_g1Spec};
+    return {prodName, det, work:_g1Work, spec:_g1Spec, gateOptNote};
   }
   // Render grouped (ชุดหลายส่วน) — default ประโยคเดียว / qSplit แยกราคา
   window._qGroupKeys = Object.keys(groups);
@@ -5897,7 +5898,9 @@ function genQuote(keepNo){
     // G1 ใบ 5 บล็อก: ถ้า itemDetail แยก work/spec → ประกอบ marker ###รายละเอียด### (qrow render รายการ→OPTION→หมายเหตุ→รายละเอียด) · อื่น=flat det
     let _qDet=d.det;
     if(d.work && d.spec && (d.work.length||d.spec.length)){ _qDet=d.work.join('\n')+'\n###รายละเอียด###\n'+d.spec.join('\n'); }
-    rows += qrow(rowIdx, _qHdr, _qProd, _qSz, _qDet, it.qty, it.r.sell, line, it.r.addonLines, '', it.itemOptDelta||0, it.note, (it.div?it.div.dataset.id:''));
+    // G2-12: ต่อ gateOptNote (OPTION ลายไม้) เข้า note → qrow render บล็อก OPTION (แดง) ระหว่างรายการ↔รายละเอียด
+    const _itemNote = [it.note, d.gateOptNote].filter(Boolean).join('\n');
+    rows += qrow(rowIdx, _qHdr, _qProd, _qSz, _qDet, it.qty, it.r.sell, line, it.r.addonLines, '', it.itemOptDelta||0, _itemNote, (it.div?it.div.dataset.id:''));
   });
   const discPct=clampDisc(parseFloat(document.getElementById('discPct').value)||0, subtotal);
   const discFlat=parseFloat(document.getElementById('discFlat').value)||0;
