# ส่งคิวกลาง — G4 ฝาตู้ Future Tech: ใบ 5 บล็อก + ตัด jargon + รหัสสี

> จาก แชท A (G4) · 17 มิ.ย. 2026 · **ส่งคิวกลาง** (ห้ามส่ง Chat B ตรง) · โค้ด verify sandbox แล้ว **13/13 · ราคาไม่เพี้ยน**
> เจอจาก quote-option-audit: ต้นแบบ 5 บล็อกทำแค่ "ตู้อลู (cabinet)" **ตก "ฝาตู้ (future_tech)"** → ใบฝาตู้ยังเป็นแบบเก่า

## ปัญหาที่พบ (ฝาตู้)
1. ใบยังกองใต้ "รายละเอียดงาน" (แบบเก่า) — ไม่ใช่ 5 บล็อก
2. ชื่อในใบมี jargon `(/บาน, max 3 บาน/ชุด)` — ไม่ควรอยู่ในใบลูกค้า
3. สีพิเศษไม่มีช่องกรอกรหัส (ตู้มี · ฝาตู้ไม่มี)

## ผลหลังแก้ (verify แล้ว)
```
ฝาตู้ Future Tech 2 บาน (1.0 × 1.4 ม.)
- บานหน้า Future Tech 2 บาน
รายละเอียด
- สี: อบพิเศษ · รหัส KL10232A        (ไม่กรอกรหัส → "· ⚠ ต้องระบุรหัสสี")
                                     (ขาว/ดำ → "สี: ขาว/ดำ (มาตรฐาน)")
```

## โค้ดก็อปวางได้เลย (3 จุด · public/calculator/index.html)

### จุด 1 — buildItemOpts future_tech: เพิ่มช่องรหัสสี
หาบล็อก `if(p.future_tech){...}` (มี `class="o-ftcolor"`) · **เพิ่ม 1 บรรทัด** ก่อน `}` ปิดบล็อก (ต่อท้ายบรรทัด select สีฝาตู้):
```js
    h+='<label class="opt cab-detail" style="flex:1 1 100%;">รหัสสี (เฉพาะสีพิเศษ/อบ) <input type="text" class="o-ftcolorcode" placeholder="เช่น KL10232A" style="width:200px;height:32px;"></label>';
```

### จุด 2 — readItem future_tech: อ่านรหัสสี
หาบรรทัด `if(p.future_tech){ const _ftc=d.querySelector('.o-ftcolor'); optSel.ftcolor=_ftc?_ftc.value:'0'; }` → **แทนด้วย**:
```js
  if(p.future_tech){ const _ftc=d.querySelector('.o-ftcolor'); optSel.ftcolor=_ftc?_ftc.value:'0'; const _ftcc=d.querySelector('.o-ftcolorcode'); optSel.ftcolorcode=_ftcc?_ftcc.value:''; }
```

### จุด 3 — itemDetail future_tech: ทำ 5 บล็อก + ตัด jargon
หาบล็อก `else if(it.p.future_tech){ ... }` (itemDetail · ก่อน `else if(it.p.bar_grid`) → **แทนทั้งบล็อกด้วยโค้ดในไฟล์ `scripts/_frag-ft-detail.txt`** (vetted)
- หัว: ตั้ง `prodName='ฝาตู้ Future Tech '+_fN+' บาน'` (ตัด jargon · ขนาดต่อท้ายโดย _sz เดิม)
- รายการ: บานหน้า · สเปค: สี+รหัส (pattern COLORS+hasCode+⚠ เหมือนตู้)

## ⛔ กฎเหล็ก
- ข้อความบนใบล้วน + เพิ่ม field รหัส · **ห้ามแตะ calcUnit/ราคา**
- verify ก่อน commit: `node scripts/_verify-g4-ft.mjs` → ต้อง **13/13**
- `node scripts/golden-snapshot.mjs` → 0 เพี้ยน
- verify browser จริง: ออกใบฝาตู้ + เลือกสีพิเศษ → ดูใบ 5 บล็อก + ช่องรหัส + ⚠ ตอนไม่กรอก

## หมายเหตุคิวกลาง
- จุด 1 เป็น field ใหม่ในฟอร์ม (o-ftcolorcode) — ถ้าคิวกลางจะจัด label/ปุ่มให้เหมือนทั้งระบบ รวมแก้ตรงนี้ได้
- jargon `(/บาน, max 3 บาน/ชุด)` ยังอยู่ใน PRODUCTS L676 (ชื่อในดรอปดาวน์ฟอร์ม) — ใบไม่โชว์แล้ว · ถ้าอยากตัดในดรอปดาวน์ด้วย แก้ L676 (แต่เช็ค ref อื่นก่อน)
