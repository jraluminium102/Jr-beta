# 🎨 ใบสั่งงาน G3 — ใบ B (งานปุ่ม/UX)

> 17 มิ.ย. 2026 · จาก: แชท G3 (ตรวจ) → **คิวกลาง → แชทกลาง UX** (จัดปุ่มให้เหมือนกันทั้งระบบ) · ไม่ใช่ Chat B ตรง
> งานในใบนี้ = "หน้าตาปุ่ม" ล้วน (ดรอปดาวน์→ชิป) · ไม่แตะ logic/ราคา

## สไตล์มาตรฐาน (พี่นัทเคาะ 17มิ.ย. · แบบ ค)
**สี/วัสดุหลายตัว (≥6) ชื่อยาว = ชิปย่อชื่อ + จุดสีกลม** (ติ๊กจังหวะเดียว · มองง่าย)
- ชิป label = ชื่อย่อ (เช่น "ใส 83%") + `<span>` จุดสี ~13px
- **ค่า/ชื่อเต็มยังลงใบเสนอราคา** (เก็บ value เต็มใน hidden select · ชิปแค่ label) → เทสว่าใบออกชื่อเต็มถูก
- ม็อกอัปอ้างอิง: `docs/กลุ่ม3-หลังคากันสาดฝ้า/MOCKUP-สีโพลีตัน-ชิป-2026-06-17.html` (แบบ ค)

## งานในใบนี้ (3 จุด · ทั้งหมดเป็นดรอปดาวน์ที่เหลือใน G3)

### B1 · สีโพลีตัน 8 สี → ชิปย่อ+จุดสี
- **จุด:** `o-roofcolor` ของ roof_polyton (L~4026 · ตอนนี้เป็น `<select>` จริง ไม่ใช่ display:none)
- ย่อชื่อ: ใส 83% · ขุ่น 20% · ชาใส 30% · เขียวใส 45% · เทาออส 40% · ซิลเวอร์ 4.8% · ชาเมทัลลิค 1% · เขียวเมทัลลิค 14%
- value เต็มคงเดิม (เช่น "Clear 101 สีใส (แสงผ่าน 83%)")

### B2 · ตำแหน่งฝ้า + แนวฝ้า → ชิป (2 ตัวเลือก)
- **จุด:** `o-ceilpos` (นอก/ใน) + `o-ceildir` (ตรง/เฉียง) ในกล่องฝ้า (L~4069 · `<select>` จริง)
- ชิปธรรมดา ไม่ต้องจุดสี (ตัวเลือกน้อย ชื่อสั้น) · pattern เดียวกับ rfChips/chSetChip ที่มีอยู่

### B3 · สีชินโคไลท์ครบรหัส → ชิปย่อ+จุดสี (รอใบ A ใส่ข้อมูลก่อน)
- **ขึ้นกับ A2** (ใบ A ต้องใส่ข้อมูลสี 14 สีก่อน) → B3 ทำให้แสดงเป็นชิปแบบ ค
- ⚠️ ลำดับ: A2 (ข้อมูล) เสร็จก่อน → B3 (จัดชิป)

## ✅ เทสปิดงาน (UX)
- `node scripts/golden-snapshot.mjs` = 149 ตรง (ปุ่มล้วน ห้ามแตะราคา)
- กดแต่ละชิป → ราคา/ค่าตรงเดิม · **ใบเสนอราคาออกชื่อสีเต็มถูก** (ย่อ→เต็ม map)
- verify เบราว์เซอร์จริง + แคปรูปทุกจุด

**ปลายทาง:** คิวกลาง → แชทกลาง UX (ทำชิปสไตล์เดียวกันทั้งระบบ · ไม่ใช่ G3 แก้เอง)
