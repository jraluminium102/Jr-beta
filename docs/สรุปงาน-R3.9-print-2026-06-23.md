# สรุปงาน — ไฟล์พิมพ์ R3.9 (สูตรราคา ฉบับพิมพ์)

📄 ไฟล์: `docs/DRAFT-R3.9-print-2026-06-23.html`
📅 23 มิ.ย. 2569

---

## งานนี้มี 4 เรื่อง:

1. **ระแนง 92 รุ่น (ข้อ 38.4)** — ใส่ครบทุกรุ่น แบ่ง 16 กลุ่ม (rn1–92) แทนของเดิมที่ยุบเหลือ 18 แถวสรุป
2. **หัวคอลัมน์ซ้ำทุกกลุ่ม** — แถบสีส้มกลุ่มระแนง โชว์ชื่อคอลัมน์ (ผลิต+ติด/เฉพาะผลิต/+ซาฮารา/…) ทุกกลุ่ม เลื่อนลึกแล้วยังอ่านออกว่าช่องไหนเป็นอะไร
3. **กระจก (ข้อ 31)** — เติมครบ 66 รุ่น (เดิมโชว์แค่ ~25) + ขยายเป็น full-width
4. **หลังคา (ข้อ 33–34)** — เติมเรตจริงครบ 18 รุ่น (มาตรฐาน 4 · เมทัล 8 · ชินโค 6) + หลังคาเลื่อนเรตต่อช่วงพื้นที่

---

## ตารางสรุป

| พี่บอก | ผล | สถานะ | คำถาม |
|---|---|---|---|
| ใส่ระแนง 92 รุ่นให้ครบ | เพิ่มครบ 16 กลุ่ม rn1–92 (เรตตรง engine) | ✅ | – |
| เพิ่มหัวคอลัมน์ตรงแถบสีส้ม | ซ้ำชื่อคอลัมน์ทุกกลุ่ม (16 กลุ่ม) | ✅ | – |
| กระจกขาด เติมให้ครบ | กระจกครบ **66/66 รุ่น** | ✅ | – |
| เช็คอื่นๆ ให้ครบ | หลังคา 18 รุ่น + หลังคาเลื่อนเรต เติมครบ | ✅ | – |
| เติมชื่อสีจริงต่อรุ่นหลังคา | เพิ่มบรรทัดสีเล็กใต้ชื่อทุกรุ่น (ไวนิล/ดีไลท์/โพลีตัน 8 สี/เมทัล 3 สี/ชินโค 6 รุ่น) จาก QIG3_GROUPS + SHINCO_COLORS | ✅ | – |

---

## แหล่งข้อมูล (ยึด engine จริง — ไม่เดาเลข)
- กระจก: `GLASS[]` (index.html L627–675) — 66 รุ่น
- หลังคา: `RATES` ROOF_STD/POLY/LAM/SLIDE + IMP7–19 (L562–589) · min/รุ่นจาก PRODUCTS L725–793
- ระแนง: rn1–92 (L813–904)

---

## เพิ่ม: ทำ PDF สวย จัดหน้าเต็มไม่เว้นช่อง (รอบ PDF)

| พี่บอก | ผล | สถานะ | คำถาม |
|---|---|---|---|
| ทำ PDF สวยๆ | gen ด้วย Chrome headless `--print-to-pdf` → `docs/สูตรราคา-JR-R3.9-ฉบับพิมพ์.pdf` (6 หน้า · 1.44MB · สีพื้น/หัวตารางติดครบ) | ✅ | – |
| จัดหน้าอย่าเว้นข้าม เต็มทุกหน้า | เปลี่ยน layout grid → **multicolumn** (column-fill:auto) + ตารางใหญ่ไหลข้ามหน้า · ช่องว่างก้นหน้า 68mm→ทุกหน้าเหลือ ~4-10mm (หักขอบ) · จาก 7 หน้าโล่ง → 6 หน้าเต็มพอดี | ✅ | – |

**วิธีทำ PDF (reuse ได้):** Chrome `--headless --no-pdf-header-footer --print-to-pdf` + CSS `@media print{ print-color-adjust:exact }` ให้สีติด · `.grid{column-count:2;column-fill:auto}` + `.full/.sh{column-span:all}` เติมหน้าแน่น · วัดช่องว่างด้วย pdfjs-dist (transform[5] ต่ำสุดต่อหน้า)

---

## เพิ่ม: ปรับ layout PDF เป็น 3 คอลัมน์ + ตารางเดียวยาว (เหมือนใบเดิมที่ใช้ประจำ)

| พี่บอก | ผล | สถานะ | คำถาม |
|---|---|---|---|
| จัด PDF เป็น 3 แถว (คอลัมน์) เหมือนใบเดิม | เปลี่ยน `.grid` column-count 2→3 (gap 4mm) | ✅ | – |
| 1 ตาราง ทำเป็นตารางเดียวยาวลง ไม่แยก 2 ตาราง | แปลงตารางแยกซ้าย-ขวา (`.ts`) **18 ตาราง** → ตารางเดียวไหลลง (สคริปต์ jsdom) | ✅ | – |
| ทำดราฟมาดูก่อน | regen PDF 6 หน้า เต็มทุกหน้า (ช่องว่างจริง 4–8mm) · verify DOM: 3 คอลัมน์ · ข้อ1.1 ตารางเดียว 9 แถว · ไม่เหลือตารางแยก | ✅ รอพี่ดู | ชอบแบบนี้ไหม / ปรับอะไรอีก |

---

## เพิ่ม: ออกแบบใหม่ 3 ส่วนใหญ่ให้อ่านชัด + จัดหน้าพอดี (รอบ readable)

| พี่บอก | ผล | สถานะ | คำถาม |
|---|---|---|---|
| จัดหน้าให้สวยพอดีแต่ละหน้า | รีด margin/padding → 6 หน้า · p1–5 เต็ม (6–13mm) · p6 หน้าสุดท้ายเหลือ ~47mm | ✅ | – |
| ระแนงตัวเล็กเกินไป ขอเห็นชัด | ขยายฟอนต์ 5.5→6.3pt (ไม่ล้นขอบ 700<794px) | ✅ | – |
| หลังคาช่องใหญ่เกิน ทำกล่องเล็กแบบหน้า1 | แปลงตารางยักษ์ → 18 กล่องเล็ก (1 รุ่น/กล่อง · ตาราง ช่วง-ราคา แนวตั้ง) + ของเสริม ไหลใน 3 คอลัมน์ | ✅ | – |
| กระจกอ่านยากติดกันหมด ออกแบบใหม่ | จัด 4 กล่องตามหมวด (ใส/สี 13 · เทมเปอร์ 23 · ลามิเนต 24 · พิเศษ 6 = 66) · 1 รุ่น/บรรทัด (ชื่อ/ทุน/+ขาย) | ✅ | – |

**ดึงค่าจาก engine จริง** (GLASS regex 66 ตัว ชื่อจริงเป๊ะ) · เรตหลังคา/ระแนงเท่าเดิมทุกตัว

---

## เพิ่ม: ห้ามตารางขาดหน้า + เลิกตารางเต็ม (รอบ no-split)

| พี่บอก | ผล | สถานะ | คำถาม |
|---|---|---|---|
| ตารางตกหน้า อ่านงง (หลังคาเลื่อน/มุ้งขาดครึ่ง) | CSS กลับเป็น `break-inside:avoid` ทุกกล่อง/ตาราง · **กล่องสูงสุด 71mm < หน้า 282mm** → ทุกตารางจบในหน้าเดียวแน่นอน | ✅ | – |
| อย่าทำตารางเต็ม ชอบตารางเล็ก | ระแนง 92 → **16 กล่องกลุ่ม ไหล 2 คอลัมน์** · กระจก → ซอย 6 กล่องเล็ก · หลังคาเลื่อน → กล่องเล็กตารางแนวตั้ง | ✅ | – |
| จัดหน้าพอดี | 6 หน้า · p1,p4,p5 เต็ม · p2 เหลือ ~30mm (รอยต่อ section · หัวข้อเต็มแถบบังคับขึ้นบรรทัดใหม่) · p6 หน้าสุดท้ายว่าง | 🟡 | ช่องรอยต่อ section รับได้ไหม? (เลี่ยงยากถ้าห้ามตารางขาด) |

**หลักการ:** ตารางใหญ่ทุกตัวซอยเป็นกล่องเล็ก ≤71mm → ไม่มีทางขาดข้ามหน้า · แลกกับช่องว่างเล็กน้อยตรงรอยต่อหมวด (ตารางที่ไม่พอที่กระโดดทั้งก้อน)

## งานค้าง
- 🟡 รอพี่นัทดู PDF (ไม่ขาดหน้า + ตารางเล็ก) → รับช่องรอยต่อ section ได้ไหม หรืออยากให้รีดอีก
- (ออปชั่น) จุดสีกลม swatch นำหน้าชื่อสีหลังคา
