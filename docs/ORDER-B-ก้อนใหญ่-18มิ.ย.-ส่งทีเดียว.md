# 🅱️📦 ใบสั่ง Chat B — ก้อนใหญ่ 18 มิ.ย. (เคาะครบ · ส่งทีเดียว)

> แชทกลางรวบทุกคิว (A-G1 · สุ่มตรวจ · OneNote · A-G6) + กรอง + เช็คชน + เคาะครบแล้ว
> **ทุกข้อในก้อนนี้ = ไม่แตะ engine ราคา/RATES/GLASS/COLORS → golden 149 ต้องนิ่งทุกข้อ**
> ทำเรียงตามลำดับล่าง · ทีละข้อ + golden ก่อน-หลัง + verify browser → เสร็จกลุ่มหยุดให้ตรวจ
> ⛔ **A2 (G6 ห้องเลือกกระจก/สีครบ · แตะราคา) = แยกรอบ ไม่อยู่ในก้อนนี้** (มติพี่นัท 18มิ.ย. · ใบสั่งแยกตามมา)

---

## 🔴 ลำดับ 1 — คิดเร็วราคาหาย (ด่วนสุด · กระทบลูกค้าจริง)
**ใบสั่งเต็ม:** `docs/HANDOFF-คิดเร็วใบเสนอ-แก้6จุด-dev-2026-06-18.html` (Z5 + Z3)
- #5 🔴 เติม `calcQuickAll();` ท้าย qiRenderG2(L2686)/G3(L2230)/G4(L2294)/G5(L2433)/G6(L2511)/G7(L2363) — **G1 ห้ามแตะ**
- #6 แยกปุ่มไวนิล/ดีไลท์ (QIG3_CATS L2207)
- #8 ย้ายปุ่มจัดการใบ (.qbtns L499-509) **ขึ้นบนสุด** (มติพี่นัท 18มิ.ย.: ไว้บนพอ · **ไม่ต้อง sticky** กันเกะกะ)

## 🟧 ลำดับ 2 — chipify สี/ผิวที่เหลือ (Z4 · เหมือนกันข้าม G)
**ใบสั่งเต็ม:** `docs/ORDER-B-รอบ1-chipifyสี+ผ้ามุ้งซ้ำ-2026-06-18.md` ข้อ A1-A4
- A1 `o-mscolor` (G5 L4534) + A2 `o-ftcolor` (G4 L4568) → ชิป+จุดสี**ชุดเดียวกัน** (COLORS≥2 · rfChipsSw)
- A3 `o-gcfinish` (G3 L1537 · 8 ตัว) → +CHIPIFY_SELECTS +CHIPIFY_BIG · A4 `o-rfsealercolor` (3 ตัว) → +CHIPIFY_SELECTS

## 🟦 ลำดับ 3 — ปุ่ม "ยกเลิกขาย" ทุกกลุ่ม (Z3+Z4 · รวม A1 จาก G6 + #7 เดิม)
- ในใบ: ตั้ง `discontinuedSuffix=''` (L5428) → เลิกเติม "(ยกเลิกขาย)" ขึ้นใบ (L5401 ตัดอยู่แล้ว · แค่เลิกเติมกลับ)
- ที่ชิปเลือกรุ่น: ลบคำ "(ยกเลิกขาย)" จาก label + ใส่ class สีจาง (opacity:.55) · **กดได้/ขายได้ปกติ**
- 4 ตัว: sliding_eseries(L657) · awning_aluinch(L660) · casement_dseries(L662) · pivot_aluinch(L669) → ทำเป็น rule เดียว (เช็ค `name.includes('ยกเลิกขาย')`)
- ⚠ ข้อความ ไม่กระทบราคา

## 🟨 ลำดับ 4 — ใบ render (Z3 · ทำเรียงทีละจุด+snapshot · โซนเดียวกัน)
- B1 (ตัดผ้ามุ้งซ้ำ imp21): `_mqNm` L5730 ตัด "ผ้ามุ้ง..." **เฉพาะเมื่อ screenFabric เลือก** (เหมือน L5420 · imp28/29 ไม่กระทบ) — "หัวซ้ำ" ไม่ใช่บั๊ก ไม่แตะ
- C2 (หลังคาสเปค G6): `g6rRoomSpec` push ชนิด+สีหลังคา → บล็อก "รายละเอียด" (ถ้ามีหลังคา)

## 🟩 ลำดับ 5 — UX ห้อง G6 + หมายเหตุ (Z6 + Z1)
- B1-G6 (รวบออปชั่นห้อง): `g6rRenderOpt` L6635 รวบ **มือจับ·ล็อก·มุ้ง·คาดตาราง·เพิ่มอลูทึบ·คอมโพสิต** เป็นกล่อง "🔧 ใช้บ่อย" เดียว (มติพี่นัท 18มิ.ย.: รวมอลูทึบ/คอมเข้าใช้บ่อยด้วย) · ย้าย สีเฟรม/ทาสี/ครอบวงกบ/ดรอปพื้น ลงกล่องพับ · **แค่ลำดับ section ไม่แตะ logic** · ดราฟ `docs/กลุ่ม6-กั้นห้องกระจก/DRAFT-G6-UX-จัดกลุ่มออปชั่นห้อง-2026-06-18.html`
- C1 (เพิ่ม 2 preset หมายเหตุ · กลุ่ม "ไม่รวมในราคา" L1932): **เทียบทะเบียนแล้ว 2/4 มีอยู่ (ย้ายแอร์ L1939 · ลงเข็ม/เทพื้น L1937) เพิ่มแค่ 2:**
  - `'ราคาที่เสนอไม่รวมงานผนังสมาร์ทบอร์ด (ลูกค้าดำเนินการเอง)'`
  - `'ราคาที่เสนอไม่รวมงานระบบน้ำ/ประปา (ลูกค้าดำเนินการเอง)'`

## 🟢 ลำดับ 6 — UX รอง (ทำท้าย ถ้ามีเวลา)
- #9 กระจกเทมเปอร์/ลามิเนต ชิปยาว → รุ่นย่อยเป็น dropdown (หมวดสั้นคงชิป) [Z4]

---

## ✅ เช็คทุกข้อ
- golden 149 นิ่ง (ทั้งก้อนไม่แตะเงิน) · check-g1..g7 🔴0
- A1mscolor+A2ftcolor หน้าตาชิปเหมือนกัน · คิดเร็วกดชิปแล้วราคาไม่หาย · ยกเลิกขายขึ้นใบไม่มีคำ+ชิปจางกดได้ · imp21 ผ้าโชว์ที่เดียว

## ⛔ ไม่อยู่ในก้อนนี้ (แยก/ปิด)
- **A2 G6 กระจก/สีห้อง** = รอบแยก (แตะราคา · re-baseline golden) — ใบสั่งตามมา
- B2 ชนิดการเปิด = เป็นชิปแล้ว (รอ G6 ชี้จุด) · ฝาก G1 owner (ผนังเบา/ปุ่มยาว) · ปิด: ระแนงเกล็ด/ราวเหล็ก/OPTION accordion
