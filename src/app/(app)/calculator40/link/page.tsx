import { redirect } from "next/navigation";

// หน้า "ลิงก์ สโตร์ ↔ ใบตัด ↔ คิดราคา 4.0" — ปิดไว้ (เจ้าของสั่ง 2 ก.ย.69)
//
// ทำไมปิด: หน้านี้ให้ "แก้สูตรจากเว็บ" ซึ่งต้องมีตาราง calc_line_overrides (migration 0134)
//   ตารางนั้นไม่เคยลงในฐานข้อมูลจริง กดแก้ทีไรก็เด้ง "ยังไม่ได้รัน migration" ทุกครั้ง
//   เจ้าของเคาะแล้วว่าไม่เอาวิธีนี้ และจะไม่รัน migration → หน้านี้จึงเป็นทางตันอย่างเดียว
//
// ⚠ ทำไมไม่ลบทิ้ง: โค้ดหน้านี้ (page.tsx.disabled + LinkClient.tsx) ยังเก็บไว้เผื่อเปลี่ยนใจ
//   แต่ห้ามให้คนเดินเข้ามาเจอปุ่มที่กดไม่ได้ → ส่งไปหน้าเทียบที่ใช้ได้จริงแทน (ไม่ง้อ migration)
export default function LinkPageDisabled() {
  redirect("/calculator40/compare");
}
