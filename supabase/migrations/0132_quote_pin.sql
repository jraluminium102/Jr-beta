-- ============================================================
-- 0132 · รหัสผ่านหน้าคิดราคาประเมิน (public quick-quote)
--   หน้า /quote (เซลล์คิดราคาหน้างานในมือถือ · ไม่ต้อง login) ใส่รหัสนี้ครั้งเดียวจำในเครื่อง
--   ตั้ง/เปลี่ยนได้ทุกเมื่อ:  update app_config set value='รหัสใหม่' where key='quote_pin';
--   ค่าเริ่มต้น = jr2026  → **เจ้าของควรเปลี่ยนเป็นรหัสของตัวเอง**
-- ============================================================
insert into public.app_config (key, value)
values ('quote_pin', 'jr2026')
on conflict (key) do nothing;   -- ไม่ทับของเดิมถ้าตั้งไว้แล้ว

notify pgrst, 'reload schema';
