-- ============================================================
-- JR Beta — 0022 เพิ่มช่อง "ช่างผลิต" (producer_note) ในตารางคิวผลิต
-- idempotent · รันหลัง 0021
-- ============================================================
-- เหตุผล: ช่างต้องการตารางคิวผลิตที่โน้ตชื่อคนผลิตได้ + เลื่อนวันได้
-- ไม่ใช้ productions.notes (เพราะ trigger 0021 จะสร้าง issue อัตโนมัติเมื่อ notes เปลี่ยน)
-- จึงเพิ่มคอลัมน์แยก producer_note สำหรับโน้ตคนผลิตโดยเฉพาะ
-- ============================================================

alter table public.productions
  add column if not exists producer_note text;

comment on column public.productions.producer_note is 'ชื่อ/โน้ตช่างผลิตที่รับผิดชอบงานนี้ (แสดงในตารางคิวผลิต)';
