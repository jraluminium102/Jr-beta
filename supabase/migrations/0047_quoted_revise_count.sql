-- ============================================================
-- 0047: snapshot จำนวนรอบแก้แบบ ณ ตอนส่งใบเสนอ (quoted_revise_count)
-- ------------------------------------------------------------
-- ปัญหา: งานที่ "ส่งใบเสนอ แล้วแก้แบบในวันเดียวกัน" (เช่น JR2026-159 คุณถนอมศรี)
--        เทียบ design_end > quote_sent_date แยกลำดับไม่ได้ เพราะเป็นวันเดียวกัน
--        → งานแก้แบบไม่เด้งกลับเข้าเช็คลิสต์ใบเสนอ
--
-- วิธีแก้: จำ design_revise_count ณ ตอนส่งใบเสนอล่าสุดไว้
--        ถ้า design_revise_count (ปัจจุบัน) > quoted_revise_count
--        = แบบถูกแก้ "หลัง" ส่งใบเสนอ → ใบเสนอเก่า ต้องทำใหม่ → เด้งกลับเข้า active
--        แม่นยำ ไม่อิงวันที่ · เคลียร์เองเมื่อส่งใบเสนอใหม่ (snapshot ใหม่ = ค่าปัจจุบัน)
--
-- default 0: งานที่เคยแก้แบบทั้งหมดจะ surface ขึ้นเช็คลิสต์รอบเดียว (ปลอดภัยกว่าซ่อน)
--            พอเซลล์ส่งใบเสนอใหม่ครั้งเดียว → snapshot อัปเดต → หายไปเอง
-- idempotent
-- ============================================================

alter table public.jobs
  add column if not exists quoted_revise_count int not null default 0;

comment on column public.jobs.quoted_revise_count is
  'design_revise_count ณ ตอนส่งใบเสนอล่าสุด — ถ้า design_revise_count > ค่านี้ แปลว่าแบบถูกแก้หลังเสนอราคา ต้องรีเควต (ใช้แทนการเทียบวันที่ กันเคส same-day)';

notify pgrst, 'reload schema';
